# 开发交接：日历页相关功能（新对话请先读这一份）

> 生成时间：2026-09-22　分支：`feat/align-get-brain`
> 上一轮止于提交 `96e0439`（批次 123），工作区**干净**

---

## 0. 一句话现状

日历页（`lib/pages/home/calendar/`）已经有**月/周视图、待办/时间线双模式、
活跃度标记、年月选择器、日程详情页、快速添加面板、重复规则、到点提醒**
这一整套骨架，且有 11 个测试文件覆盖。要做的是**完善与打磨**，不是从零搭。

---

## 1. 当前质量基线（已实测，新对话可直接信任）

| 检查 | 命令 | 结果 |
| :-- | :-- | :-- |
| 静态分析 | `flutter analyze --no-pub` | **0 error**（仅 5 条存量 info，见 §6） |
| Flutter 测试 | `flutter test --no-pub --exclude-tags integration` | **767 全绿**（1 skipped = integration） |
| Rust 测试 | `cd rust; cargo test --lib` | **21 通过 / 1 ignored**（连跑两次稳定） |
| Android 构建 | `flutter build apk --debug --no-pub` | ✅ 通过 |
| 真机 | 荣耀 `A2NMVB1806003756`（ELZ-AN10 / Android 14 / arm64-v8a） | 已装最新 debug 包 |

> 本机 flutter 路径：`D:\flutter\3.41.0\bin\flutter.bat`；
> 需要时先 `$env:PUB_HOSTED_URL='https://pub.dev'`（AGENTS.md 有说明）。

---

## 2. 上一轮（批次 103–123）做了什么（背景，不必重做）

这一段主要是**端侧语音转写 + AI 分流/记忆**，日历页改动很少。要点：

| 批次 | 内容 |
| :-- | :-- |
| 103–108 | 端侧实时语音转写（Android JNI + Windows Rust/FFI）、本地标点、录音电平归一化 |
| 109–111 | **三级分流**（规则打分 → 朴素贝叶斯兜底 → 只送相关片段）、结果缓存、跨笔记批量抽取 |
| 112 | 语音输入改为「点话筒进页面 → 由用户按播放键才开始录」 |
| 113–118 | 设置页大整理（分区重排、删冗余、底部导航改二级页、第三方 Key 归位） |
| 116–117 | 腾讯云混元签名链路**整体退役**，旧版 AI 助手页删除 |
| 119–120 | **分层记忆**（画像/长期记忆/技能手册，Markdown 文件）+ 建议式自学习 |
| 121 | AI 上下文统一（`PromptContext`：笔记就地 + 记忆全局） |
| 122–123 | 对话流去掉左右留白、身份标签移到气泡上方 |

---

## 3. 日历页代码地图

| 文件 | 行数 | 职责 |
| :-- | :-- | :-- |
| `lib/pages/home/calendar/calendar_view.dart` | 839 | 页面本体：头部、月网格、周切换、模式切换、待办列表、时间线、空态、卡片 |
| `lib/pages/home/calendar/calendar_logic.dart` | 209 | 数据加载与状态切换、年月跳转 |
| `lib/pages/home/calendar/calendar_state.dart` | 33 | `Rx` 状态（当前月/日、showWeek、mode 等） |
| `lib/features/schedule/models/schedule.dart` | 182 | 日程/待办模型（含子任务 `ScheduleSubtask`） |
| `lib/features/schedule/schedule_repository.dart` | 133 | 日程读写（Isar） |
| `lib/features/schedule/schedule_recurrence.dart` | 95 | 重复规则 |
| `lib/features/schedule/views/schedule_detail_page.dart` | 633 | 日程详情（可编辑、子任务） |
| `lib/features/schedule/views/quick_add_schedule_sheet.dart` | 209 | 快速添加面板 |
| `lib/features/activity/calendar_activity.dart` | 47 | 活跃度（月格上的点） |
| `lib/features/reminder/*` | — | 到点提醒引擎与调度（`reminderEnabled` 开关在设置→工具） |

入口：底部导航「日历」→ `home_view.dart` 里的 `CalendarPage()`。

**相关测试（改动后必须保持通过）**

```
test/features/schedule_repository_test.dart
test/features/schedule_recurrence_test.dart
test/features/schedule_detail_page_smoke_test.dart
test/features/todo_schedule_surface_test.dart
test/features/calendar_activity_test.dart
test/features/reminder_engine_test.dart
test/features/reminder_scheduler_test.dart
test/features/digest_scheduler_test.dart
test/features/feed_scheduler_test.dart
test/features/task_scheduler_test.dart
test/pages/calendar_page_mobile_test.dart   ← 手机端布局/溢出用这个兜底
```

---

## 4. 建议的切入顺序（新对话可直接照做）

1. **先跑一遍基线**：`flutter analyze` + `flutter test`，确认 767 全绿再动手；
2. **让用户描述具体问题**（他最了解哪里难用）。常见打磨方向：
   - 月格信息密度（点多/挤）、周视图与月视图切换的连贯性；
   - 待办与时间线的取舍与合并显示；
   - 日程详情页与「抽取待办」（AI 分流产出）之间的联动；
   - 提醒的可靠性（`reminderEnabled` 已开，但真机到点是否弹出需实测）；
3. **每改一处就跑** `test/pages/calendar_page_mobile_test.dart`（防溢出）；
4. **真机验证**：`adb -s A2NMVB1806003756`，本机有截图+识图流程（见 §5）。

---

## 5. 真机与调试经验（能省很多时间）

- 真机序列号：`A2NMVB1806003756`（荣耀），包的 applicationId 是
  **`cn.yooss.moodiary.debug`**（读取应用私有目录要 `run-as cn.yooss.moodiary.debug`）；
- 装机：`adb -s <serial> install -r -t build\app\outputs\flutter-apk\app-debug.apk`；
- **截图识图**：主模型无视觉，用 `see` skill：
  `bash -c "bash /mnt/c/Users/raych/.codex/skills/see/scripts/see.sh 'C:\path\shot.png'"`
  提示：**等界面稳定 3–5 秒再截**，否则容易截到过渡动画造成误判；
- **坐标点击容易偏**（Flutter 自绘，uiautomator 拿不到控件树）。
  可直接加 Intent 钩子驱动（本轮做过范例：`MainActivity` 的
  `--es ASR selftest` / `streamtest`），比盲点更可靠；
- 应用私有目录：模型 `files/asr/`、记忆 `files/ai-memory/`、音频 `files/audio/`。

---

## 6. 已知存量问题（**新对话不要误当自己改坏的**）

`flutter analyze` 稳定的 5 条 info（历史遗留，未修）：

1. `lib/features/crm/crm_entity_detail_view.dart:561` — `prefer_if_null_operators`
2. `lib/features/quick_capture/quick_capture_view.dart:438` — `use_build_context_synchronously`
3. 同上 `:455`
4. `lib/features/smart_canvas/smart_canvas_page.dart:1618` — 同上
5. `lib/utils/theme_util.dart:337` — `CorePalette` 已废弃

另外两点：

- `cargo test` 偶发 `api::event_bus` 两个用例失败（测试顺序/并发导致），**重跑即过**；
  本轮连续跑两次均通过。若遇到，先重跑再判断是否真回归。
- 分支**领先 origin 61 个提交**，一直没有推送（用户未要求推送，别自作主张 push）。

---

## 7. 项目硬性规则（AGENTS.md 摘要，别踩）

1. 新功能代码放 `lib/features/`（Flutter）或 `rust/src/`（Rust），**不要散落进 `lib/pages/`**；
   —— 注意：日历页现存的 `lib/pages/home/calendar/` 是上游结构，
   **新增能力建议放 `lib/features/schedule/`**，页面里只做接线；
2. 提交规范 `<type>(<scope>): <subject>`，并在 `docs/开发进度.md` 追加一段；
3. 改 Flutter 必须 `flutter analyze` + `flutter test`；改 Rust 必须 `cargo test`；
4. 每个改动批次结束前，**真机走查**（用户明确要求过截图佐证）；
5. 禁止用 `cat`/shell 写文件，源码编辑用 `apply_patch`。

---

## 8. 一句话给新对话的开场建议

> 「继续完善日历页。先读 `docs/开发交接-日历页.md` 和 `docs/开发进度.md` 的批次 103–123 段，
> 跑通 `flutter analyze` + `flutter test` 基线，然后按 §4 的顺序切入，每改一处做真机走查。」
