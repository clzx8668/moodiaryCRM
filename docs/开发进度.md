# 开发进度跟踪

> 主协调维护。每个任务完成后更新状态：🟡进行中 ✅完成 ❌阻塞。

## Phase 0：基座与环境 ✅

| 任务 | 状态 | 备注 |
| :-- | :-- | :-- |
| P0.1 工具链验证 | ✅ | Flutter 3.32.0 / Dart 3.8.0 / Rust 1.96 / JDK 17 |
| P0.2 依赖源方案 | ✅ | pub.dev 直连 + ghfast.top git 镜像（进程级注入） |
| P0.3 计划与岗位文档 | ✅ | docs/二次开发计划.md、docs/开发岗位配置.md、AGENTS.md |
| P0.4 基线测试 | ✅ | flutter analyze 0 问题；cargo test 13/13；server gradlew build 成功 |
| P0.5 工作流约定 | ✅ | 见 docs/二次开发计划.md 第 6 节 |

## Phase 1：基座与记录核心

| 任务 | 状态 | 负责 | 备注 |
| :-- | :-- | :-- | :-- |
| P1.1 Block 协议与模型 | ✅ | arch 代 data | lib/features/block/models/block.dart + AppMetadata；测试 7 用例绿 |
| P1.2 数据迁移 v1→v2 | ✅ | arch 代 data | lib/persistence/migration.dart；幂等 + 迁移历史；测试 5 用例绿 |
| P1.3 FFI 契约 | ✅ | rust_engineer | rust/src/api/ffi_api.rs（API_VERSION=1，扁平 DTO） |
| P1.4 Rust 同步引擎骨架 | ✅ | rust_engineer | rust/src/sync/ 9 模块 + 24 新测试；cargo test 37/37 绿 |
| P1.5 快速收集面板 | ✅ | arch 代 flutter | lib/features/quick_capture/ + 首页 FAB 集成；analyze 0 问题 |
| P1.6 记录列表块视图 | ⬜ | - | 待启动 |
| P1.7 全局搜索增强 | ⬜ | - | 待启动 |
| P1.8 Twenty CRM 增量同步 | ⬜ | - | 待启动 |
| P1.9 WebDAV 文件同步对齐 | ⬜ | - | 待启动 |
| P1.10 同步日志 | ⬜ | - | 待启动 |

> 备注：data_engineer / flutter_engineer 子代理启动后未产出，由主协调（arch）直接实现对应任务。

### 验收结果快照（2026-08-19）

- `flutter analyze`：No issues found
- `flutter test`：17/17 全绿（含新增 Block/迁移 12 用例）
- `cargo test`：37/37 全绿（基线 13 + 新增 24）
- `gradlew build`（server）：BUILD SUCCESSFUL
- `flutter build windows --debug`：首次执行卡在 cargokit/CMake 阶段（疑似网络），已中止；列为 M1 出口项

## 下一步（待启动）

- P1.6 记录列表块视图 / P1.7 全局搜索增强 / P1.8 Twenty CRM 增量同步 / P1.9 附件管线 / P1.10 同步日志
- M1 出口：`flutter build windows --debug` 真实构建通过后联调

## 第二轮：Twenty CRM 打通（2026-08-19）

用户提供 Twenty 测试环境（http://10.200.245.54:3000 + API Key），已完成：

| 任务 | 状态 | 备注 |
| :-- | :-- | :-- |
| P1.8a Twenty 连通性验证 | ✅ | GraphQL introspection 确认对象模型；发现自定义对象（合同/回款/发票/提成） |
| P1.8b 安全配置 | ✅ | config/twenty.local.json（gitignored）+ example；令牌未入 git |
| P1.8c TwentyApiClient | ✅ | GraphQL 分页/创建/更新/删除 + 429/5xx 指数退避（2s×5 次）+ 复数对象映射 |
| P1.8d CRM 缓存模型 | ✅ | CrmEntityCache Isar 表（twentyId 索引/快照/软删/版本） |
| P1.8e CrmSyncService | ✅ | 连接测试/全量拉取/创建/删除/本地搜索/统计 + 结构化同步日志 |
| P1.10 同步日志服务 | ✅ | SyncLogService：内存 500 条环形 + JSONL 落盘 + 级别/时间过滤 |
| 真实环境集成测试 | ✅ | flutter test --tags integration 通过（拉取+建删闭环） |
| CLI 自检 | ✅ | dart run tool/twenty_sync_check.dart：7 公司/5 联系人/6 商机 |
| CRM 同步页面 | ✅ | 设置→CRM 同步：配置/测试/全量同步/缓存搜索/日志（secure storage 存令牌） |
| 服务端 Ktor 代理 | ✅ | /api/twenty/* 健康/列表/创建/删除；8 测试绿；真实链路 curl 验证通过 |
| 自定义业务对象 | ✅ | 合同/回款/发票/提成 全量同步（均有真实数据） |
| P1.7 全局搜索服务 | ✅ | GlobalSearchService：日记/Block/CRM 跨模块；2 用例绿 |

验证结果：flutter analyze 0 问题；单测 28/28；集成测试 1/1。

第三轮验证（含第二轮增量）：flutter analyze 0 问题；单测 30/30；集成 1/1；server gradle test 8/8。

## 待办（需用户协调或后续轮次）

- P1.6 记录列表块视图：UI 较重，下一轮做（现有 list/grid 视图可用）
- frb 绑定重新生成（flutter_rust_bridge_codegen），把 Rust 同步引擎从轮询占位升级为事件流
- Twenty 自定义对象 UI（合同/回款/发票/提成 管理页）
- 日历热力图/时间轴（P2.1/P2.3）、模块开关（P2.8）

## 第三轮：日历热力图 + 自定义对象浏览 + 模块开关 + 附件管线（2026-08-19）

| 任务 | 状态 | 备注 |
| :-- | :-- | :-- |
| P2.1 日历热力图升级 | ✅ | CalendarActivity 纯函数：字数(0.5)/Block 数(0.3)/心情(0.2) 综合评分归一化；3 用例绿 |
| 自定义对象浏览 | ✅ | CRM 同步页对象类型页签（客户/联系人/商机/任务/合同/回款/发票/提成） |
| P2.8 模块开关 | ✅ | 设置→模块开关（CRM/知识库/日历）；CRM 入口随开关显隐 |
| P1.9 附件管线 | ✅ | AttachmentManager：Attachments/Images\|Documents/YYYY/MM + metadata.json + 孤立扫描/清理；快速收集文档接入；设置页"清理孤立附件"入口；3 用例绿 |
| P1.6 记录列表块视图 | ✅ | ViewModeType.block：Block 卡片流（类型图标/内容预览/所属日记/时间），支持分类过滤 |
| 同步对账 | ✅ | ReconcileResult：远端缺失/本地过期差异报告；CRM 页"对账"按钮 + 详情弹窗；2 用例绿；真实环境验证通过 |
| P1.10 同步日志页 | ✅ | 设置→同步日志：级别筛选（INFO/WARN/ERROR）+ 一键清空 |
| P2.3 时间轴合并 | ✅ | 日历时间轴：日记 + CRM 记录（客户/合同/回款/发票等）按时间合并降序，类型配色 |
| 业务对象管理页 | ✅ | CRM 页右上角入口：8 类对象分页签浏览（名称/关键字段摘要/详情快照）+ 单对象同步 |

第五轮验证：flutter analyze 0 问题；单测 38/38；集成 1/1（含对账）。待办收敛为：frb 绑定升级、快速收集富文本编辑器、WebDAV 附件增量同步细化。

## 架构决策（2026-08-19）：全局放弃 flutter_quill，统一 Markdown + 智能块

用户明确：全局放弃 Quill 富文本，用智能块封装功能内容块（Notion/AI 对话式），
内部文本只保留 Markdown 一种格式，简化结构同时保持丰富表达。

已实施：

| 项 | 说明 |
| :-- | :-- |
| Delta→Markdown 转换器 | lib/features/block/delta_to_markdown.dart（纯 Dart，无 quill 依赖）：加粗/斜体/删除线/链接/标题/引用/列表/图片/视频/音频/代码块 |
| 数据迁移 v2→v3 | 启动时把旧 Delta 内容转 Markdown、type 归一为 markdown、同步首个 text Block；幂等 + 迁移历史（db_version=3） |
| 编辑页 | 移除 QuillEditor/QuillSimpleToolbar；统一 Markdown 编辑器 + 预览切换 + MarkdownToolbar；保存时同步 Block |
| 渲染 | DiaryRender 改为纯 Markdown；新增 SmartBlockView（Notion 式 Block 列表渲染：文本/待办/图片/图表/代码/智能实体）接入详情页 |
| 依赖 | 移除 flutter_quill / flutter_quill_extensions；删除 quill_embed 组件 |
| 入口 | 首页 FAB 简化为"快速收集"（主按钮）+ "新建日记"（Markdown）；快速收集类型归一 markdown |

验证：analyze 0 问题；单测 47/47；集成 1/1。

注意：旧版（v2 前）数据库首次启动会自动完成 v1→v2→v3 全部迁移，无需手工操作。

## 版本升级与 Isar → Drift 迁移（2026-08-20）

用户决策：放弃 Isar，迁移 Drift(SQLite)，避免长期版本升级冲突；Flutter 升至 3.41.0（不锁死）。

| 项 | 说明 |
| :-- | :-- |
| Flutter/Dart | 3.32.0 → **3.41.0 / Dart 3.11**（.fvmrc 同步）；meta 1.17.0、material_color_utilities 0.13.0 随 SDK 升级 |
| 数据库 | Isar 4.0-dev → **Drift 2.30 + drift_flutter + sqlite3_flutter_libs**；7 张表（Diaries/Categories/Fonts/Blocks/AppMetadata/CrmEntityCaches/SyncRecords） |
| 工具链 | build_runner 2.14.0（build 4 / analyzer 8）、drift_dev 2.30、json_serializable 6.14.1、flutter_gen 5.15.0 |
| 代码生成 | 移除 freezed（6 个模型转普通 json_serializable 类）；Drift @DataClassName 避免与业务模型重名 |
| IsarUtil | 保留全部公开接口，内部切换 Drift（含内存缓存支撑少量同步调用点） |
| 迁移服务 | 重写为 Drift 实现，v1→v2→v3 逻辑不变（SQLite VACUUM 导出） |
| 测试 | 全部改用 Drift 内存库（NativeDatabase.memory）；单测 45/45 |
| Windows 构建 | 应用指南补丁：windows_cpp_prefix.h、runner /FI /utf-8 /wd4819、gal WinRT 链接、generated_plugin_registrant 预包含；修复 fvp mdk-sdk 下载（gigenet 镜像）与 install prefix 残留缓存 |
| 构建验证 | ✅ `flutter build windows --debug` 产出 mood_diary.exe；真实 Twenty 集成测试通过 |

遗留说明：
- 旧 Isar 数据库文件不再自动导入（dataMigration 为 no-op），以 SQLite 全新库启动；v1→v2→v3 数据迁移对新库执行。
- fvp 插件需要 mdk-sdk（约 12MB，sourceforge gigenet 镜像可快速下载），已放至 ephemeral 插件目录；新机器可用 `FVP_DEPS_URL` 指向 GitHub Release。

## 运行期修复（2026-08-20 二轮）

| 问题 | 修复 |
| :-- | :-- |
| frb Content hash mismatch（1525123801 vs 1609532906） | 升级 flutter_rust_bridge 2.9.0 → 2.11.1（pubspec + Cargo.toml），用 cargo 安装 codegen 2.11.1 重新生成绑定；需 nightly 工具链 + cargo-expand；sync 模块改为 Cargo feature（`sync-engine`）避免 codegen MIR 崩溃；ffi_api 与 sync 解耦 |
| 快速收集保存后不关闭 | `Get.isBottomSheetOpen` 对 showModalBottomSheet 无效，改为保存成功即 `Get.back()` |
| CRM 模块不可见 | 首页导航新增 CRM 页签（CrmHomePage：缓存统计 + 全量同步 + 最近更新 + 管理入口）；控制器安全存储读取加 try/catch 防空白 |
| 首页与开发文档对齐 | 快速收集 FAB + 块视图 + 全局搜索 + CRM 页签均已就位 |

验证：哈希两侧一致（-2003543117）；analyze 0 error；单测 45/45；`flutter build windows --debug` 成功；运行冒烟通过。

## UI 迭代：快速收集重构 + 中间详情页（2026-08-20 三轮）

任务分解与验收标准见 [UI迭代任务.md](./UI迭代任务.md)，本轮回合实现：

| 项 | 状态 |
| :-- | :-- |
| A 快速收集面板 | ✅ 单一圆角框；占位「发消息或按住说话」；空内容长按激活语音（占位）；有内容长按失效；语音/键盘切换；模板列表（待办/打卡/扩写/润色/会议记录/翻译）保存打标签 + AI 占位钩子；@ 占位；圆圈加号附件（图片/声音/文档/其他）缩略图 + 删除 + 类型提示短句；有内容显示纸飞机发送 |
| B 中间详情页 | ✅ 卡片点击已进详情页（回归确认）；Markdown 块可点击进入编辑器；编辑返回自动刷新；todo/图片块占位 |

验证：analyze 0 error（2 条既有 info）；单测 45/45；Windows 构建成功；运行冒烟通过。

## Phase 2 / 3 / 4

见 [二次开发计划.md](./二次开发计划.md) 第 3 节，暂未启动。

## P2.7 智能详情页 + 双模态架构（2026-08-20 第四轮）

用户确认设计（详见 [智能详情页-双模态架构设计.md](./智能详情页-双模态架构设计.md)），
语音方案保持占位，其余按推荐推进。分支：`feat/P2.7-smart-canvas`。

| 任务 | 状态 | 说明 |
| :-- | :-- | :-- |
| P2.7.0 双模态数据层 | ✅ | `Blocks.meta_json` 列（v3→v4 迁移，幂等 + 历史）；`BlockMeta`（source/syncStatus/aiTemplate/entityType/title）；`MarkdownProjection` 投影纯函数（7 类型 + 聚合） |
| P2.7.1 卡片容器 | ✅ | `SmartCard` 统一壳（头部来源徽标/时间/同步状态点 + 按类型内容卡 + 尾部操作），长文折叠 |
| P2.7.2 卡片路由 | ✅ | `CardActionRouter` 策略表：text/code→Markdown 编辑器（按 Block 编辑）、smartEntity→实体表单、todo→原地勾选、image→全屏预览、chart→查看 |
| P2.7.3 追加输入条 | ✅ | 底部常驻 `AppendInputBar`：文本追加 / 模板选择（带 AI）/ @与语音占位 |
| P2.7.4 实体卡 | ✅ | `EntityCard` + `EntityEditorPage`（类型/名称/状态/公司 + CRM 缓存搜索关联） |
| P2.7.5 模板 AI 真实接入 | ✅ | `AiProvider` 抽象 + OpenAI 兼容实现（DeepSeek，SSE 流式）；7 套模板 Prompt；aiStream 卡片生命周期（流式→断点持久化→按模板转正 text/todo） |
| P2.7.6 语音识别 | ⏸️ | 按用户指示保持占位（后续轮次） |
| P2.7.7 同步引擎 FFI 事件流 | ✅ | `api/sync_events.rs` + `event_bus.rs`（tokio broadcast）；`sync_progress_stream`/`ai_stream_stream`/`file_sync_stream` StreamSink 绑定重生成；Dart `SyncEventService` 启动订阅 + 页面同步状态指示 + 演示事件菜单 |

### 验证快照（第四轮）

- `flutter analyze`：0 error（仅 1 条既有 CorePalette deprecation info）
- `flutter test --exclude-tags integration`：72/72 全绿
- `cargo test --features sync-engine`：40/40 全绿（新增事件总线 3 用例）
- 环境修复：flutter_version_check.stamp 过期导致 `flutter --version` 触发 git fetch 卡死；刷新戳文件后恢复；codegen 需提升权限运行（沙箱内 flutter 探测挂起）

### 遗留（下一阶段）

- P2.7.6 语音识别真实接入（录音 → STT → 追加卡片）
- P3.x 知识库/RAG 的 LanceDB 原生接入（当前为本地向量表实现，trait 不变可平滑替换）
- 详情页进入的整篇编辑入口已保留（菜单"编辑整篇日记"）

## 真机 Android 冒烟（2026-08-20）

首次 Android 真机构建暴露并修复三条构建链路问题（荣耀 ELZ AN10 / Android 14 / arm64）：

| 问题 | 根因 | 修复 |
| :-- | :-- | :-- |
| Kotlin "different roots" 编译失败 | pub 缓存（C:）与项目（E:）跨盘符 + 增量缓存记录旧镜像路径 | `android/gradle.properties` 加 `kotlin.incremental=false` |
| cargokit "Flutter plugin not found" | Flutter 3.41 的 `FlutterPlugin` 类迁移到 `com.flutter.gradle` 包，旧匹配只认默认包名 | `rust_builder/cargokit/gradle/plugin.gradle` 兼容三种类名 |
| cargokit `getTargetPlatforms()` 缺失 | Flutter 3.41 移除该 API（internal 工具函数） | 改为按 app `abiFilters` 推导目标平台（本项目 arm64-v8a） |

另外：`android/app/key.jks`（gitignored）本机缺失，已生成本地调试密钥并写入 `android/local.properties`；fvp 的 `mdk-sdk-android.7z` 下载损坏，已经 ghfast.top 镜像重新下载（20.7MB，校验通过）。

验证结果：APK 成功打包 `libmoodiary_rust.so`（27.5MB，aarch64-linux-android 交叉编译）；应用安装并运行、无崩溃、UI 正常渲染（冷启动因 jieba 词典加载略慢）。

已知待优化（不影响主流程）：荣耀 ROM 上 `flutter run` 调试控制台（VM service 反向端口转发）连接不稳定；深色主题下详情页细节待后续打磨。

## P2.9 + Phase 3 RAG（2026-08-20 第五轮）

分支：`feat/P2.7-smart-canvas`（延续）。

| 任务 | 状态 | 说明 |
| :-- | :-- | :-- |
| P2.9 AI 流式断点恢复 | ✅ | `BlockMeta.sourceContent` 源内容快照（metaJson 自由字段）；`resumeAiBlock` 复用未完成 aiStream 卡继续生成；进入详情页提示未完成卡片，卡片尾部「继续生成」按钮 |
| P3.1 vector-db Feature + 真实 VectorIndex | ✅ | Cargo feature `vector-db`；`LocalVectorIndex`（纯 Rust 内存 + 余弦相似度 + 知识库过滤 + 可注入 embedder）；`VectorIndex` trait 不变（依赖倒置，后续可平滑替换 LanceDB） |
| P3.2 知识库管理 | ✅ | Drift `KnowledgeBases` 表（v4→v5 迁移）+ 知识库管理页（CRUD/向量计数/索引入口） |
| P3.3 增量索引 | ✅ | `BlockEmbeddings` 表（base64 f32 向量，规避 drift_dev 2.31 blob 解析崩溃——根因是列名 `text` 与 `text()` 构造器撞名）；`AiProvider.embed()`（OpenAI 兼容 /embeddings，可配 embedding 模型）；`RagService.indexBlocks/reindexBlock`；卡片编辑/删除自动清除过期向量（改删后重建） |
| P3.4 AI 对话工作台 | ✅ | `RagChatPage`：知识库选择器、流式回答、引用溯源卡片（可跳转源日记）、多轮历史 |
| P3.5 RAG 上下文构建 | ✅ | Rust `build_rag_context`（query→topK→拼装）+ Dart `RagService.buildContext`；引用格式 `[n] 文本` |
| 设置入口 | ✅ | 设置→知识库/RAG：管理页 + 对话工作台（受模块开关控制）；AI 设置增加 Embedding 模型字段 |

### 验证快照（第五轮）

- `flutter analyze`：0 error（仅 1 条既有 info）
- `flutter test --exclude-tags integration`：80/80 全绿（新增 RAG 8 用例）
- `cargo test --features "sync-engine vector-db"`：47/47 全绿（新增向量索引/RAG 7 用例）

### 待办（下一阶段）

- RAG 的 LanceDB 原生接入（`LocalVectorIndex` 内部替换即可）
- P2.7.6 语音识别真实接入
- P2.4/P2.5/P2.6 CRM 自定义表格/业务下钻/FAB；P2.2 待办聚合（Phase 2 剩余）

## AI 交互整合 + 配置迁移（2026-08-20 第六轮）

按用户要求调整：

| 项 | 说明 |
| :-- | :-- |
| AI 配置移入设置页 | 设置 → AI 助手 → AI 设置（Base URL / API Key 可见性切换 / 对话模型 / Embedding 模型）；**连接测试**：优先探测 `/models`，404 时回退最小 chat 请求，401/网络错误给出明确提示；密钥仍存安全存储 |
| AI 综合交互页 | 首页导航新增「AI」页签（桌面导航栏 + 移动底部栏）：知识库选择器（含“不使用知识库”/“管理知识库”入口）、**联网开关**、流式回答（80ms 节流 + 自动滚动 + 停止按钮）、引用溯源卡片（可跳源日记）、复制/重新生成/清空、空态建议问题、多轮历史；知识库模块开关关闭时隐藏知识库选择器 |
| 从设置移除 | 原「知识库管理」「AI 对话工作台」入口移除；`rag_chat_page.dart` 删除（由 AiHomePage 取代） |
| 主流样式打磨 | 消息居中限宽（760）、助手头像气泡、用户右侧气泡、Markdown 渲染、上下文条（知识库/联网/清空） |

验证：`flutter analyze` 0 error；`flutter test` 84/84（新增连接测试 4 用例）；Windows 构建 + 运行冒烟待确认。

## AI 崩溃与局域网同步修复（2026-08-20 第七轮）

用户 Win11 实测反馈两项缺陷：

| 问题 | 根因 | 修复 |
| :-- | :-- | :-- |
| 模板 AI 一触发即崩 | `aiProvider` 声明为 `late final`，构造函数已赋值，`loadAiProvider()` 再次赋值抛 `LateInitializationError` | 改为非 `final` 的 `late`；新增双赋值 + AI 完整生命周期（流式→转正）回归测试 |
| 局域网同步找不到服务器 | 发送端 UDP 广播依赖接收端开启「接收」标签；`getDeviceIP()` 依赖 connectivity_plus，探测异常时服务端静默不启动；无操作引导 | `getDeviceIP()` 改为直接枚举网卡、优先私网 IPv4 兜底；客户端超时给出明确指引（接收标签/同一局域网/防火墙端口）；服务端显示监听 IP:端口 |

配套：`CanvasState` 容忍组件树外构造；`NoticeUtil` 无 UI 上下文跳过 Toast；`PrefUtil` 增加测试注入接缝；`shared_preferences_platform_interface` 入 dev_dependencies。

验证：`flutter analyze` 0 error；`flutter test` 86/86；`flutter build windows --debug` ✅；运行冒烟稳定。

## 依赖源根治：flutter_inappwebview 版本锁定（2026-08-21）

用户不设 `PUB_HOSTED_URL` 时 pub 走中国镜像，会把 inappwebview 全家与 `js`/`web`
解析到不兼容的新版，导致 Windows 构建反复失败（`InAppBrowser missing implementations`、
`PlatformWebNotificationController` 缺失等）。本次在 `pubspec.yaml` 增加
`dependency_overrides` 精确锁定兼容版本（镜像源同样生效）：

```yaml
dependency_overrides:
  js: 0.6.7
  web: 1.1.1
  flutter_inappwebview_platform_interface: 1.4.0-beta.2
  flutter_inappwebview_android: 1.2.0-beta.2
  flutter_inappwebview_internal_annotations: 1.2.0
  flutter_inappwebview_ios: 1.2.0-beta.2
  flutter_inappwebview_macos: 1.2.0-beta.2
  flutter_inappwebview_web: 1.2.0-beta.2
  flutter_inappwebview_windows: 0.7.0-beta.2
```

验证：`PUB_HOSTED_URL=https://pub.flutter-io.cn` 环境下 `flutter build windows --debug` ✅（242.9s）。

## 多平台模型管理（2026-08-21）

按用户要求把单 Provider AI 配置升级为 CC Switch 样式模型管理页：

| 项 | 说明 |
| :-- | :-- |
| 数据模型 | `AiProviderConfig`：服务商名/Base URL/API Key/大语言模型/向量模型/多模态模型(预留)/语音识别模型(预留)/启用开关/优先级 |
| 存储 | `AiProviderStore`：安全存储 JSON 列表；旧版单配置自动迁移为首个 Provider（兼容既有 key） |
| 管理页 | 实验室页「AI 服务」→ 模型管理：Provider 卡片（主/备/关闭徽标、模型摘要）、启用开关、测试联通（/models 探测+chat 回退）、编辑/删除、上移/下移调主备顺序 |
| 主备切换 | `MultiProvider`：按优先级依次尝试，主模型首个输出前失败自动切换备用并在回答中注明；embed 同样 failover |
| 入口 | 实验室页 + 设置页 + AI 首页均指向模型管理页（安全存储，未并入明文机制） |

验证：`flutter analyze` 0 error；`flutter test` 96/96（新增配置模型/排序过滤/主备切换 9 用例）；`flutter build windows --debug` ✅。

## 能力分离：服务商与功能模型独立配置（2026-08-21）

用户指出"一个 API Key 通吃四类模型"不合理（DeepSeek 有对话无向量、OpenAI 向量要另配），
参照工作区 Blinko 的 Provider + 能力勾选方式重构：

| 层 | 说明 |
| :-- | :-- |
| 服务商 | 只管账号（名称/Base URL/API Key/启用/主备顺序/默认对话模型建议）；测试联通按服务商 |
| 功能模型 | 对话（通用，全部启用服务商主备自动切换）/ 向量（专用）/ 多模态（专用）/ 语音（专用）——每项独立：启用开关 + 选服务商 + 模型名 |
| 运行时 | `AiCompositeProvider`：对话走主备 MultiProvider；Embedding 走能力配置指定的专用服务商（DeepSeek 对话 + OpenAI 向量互不干扰） |
| 迁移 | 旧服务商配置自动生成默认能力（chat 用全部，embedding/vision/voice 取首个配置了对应模型的服务商） |

验证：`flutter analyze` 0 error；`flutter test` 101/101（新增能力配置/默认生成/组合装配 5 用例）；`flutter build windows --debug` ✅。

## 官方模型列表拉取与多选（2026-08-21）

按用户要求，模型名不再手填：

| 项 | 说明 |
| :-- | :-- |
| 拉取官方模型 | 服务商编辑页填好 Base URL / API Key 后点「拉取模型列表」，调 OpenAI 兼容 `/models` 接口获取全量模型 id |
| 复选框多选 | 拉取结果以 FilterChip 展示，勾选即加入该服务商的可用模型列表（支持全选/清空）；已选模型保存进 `AiProviderConfig.models` |
| 功能模型联动 | 功能模型（对话/向量/多模态/语音）的模型字段改为**下拉**：选项 = 所选服务商已勾选的模型；服务商未选模型时回退手输并提示 |
| 兼容 | 旧配置无 models 字段自动兼容（空列表 → 手输） |

验证：`flutter analyze` 0 error；`flutter test` 104/104（新增 /models 解析、models 往返 3 用例）；`flutter build windows --debug` ✅。

## AI 400 诊断与模型 fallback 修复（2026-08-21）

用户反馈 AI 对话/模板处理报 400 且日志无提醒。定位：能力分离后对话用服务商
「默认对话模型」字段，勾选官方模型列表后该字段可能为空，`toAiConfig` 兜底成
`deepseek-chat`——若该服务商是 OpenAI 等即 400 “model not found”，且错误提示
把 dio 原始文本直接弹出、服务端真实原因被吞掉。

| 修复 | 说明 |
| :-- | :-- |
| 错误提示带服务端详情 | `dioErrorDetail`/`serverMessage`：400/其他错误时解析 `error.message` 展示真实原因（如 Model Not Exist） |
| 对话模型 fallback | 默认对话模型 → 勾选模型列表第一个 → 兜底 |
| 勾选自动补默认模型 | 服务商勾选官方模型后，若默认对话模型为空自动填第一个（全选同样处理） |
| 失败写同步日志 | AI 对话/模板处理失败写入 SyncLog（operation=ai），可在「同步日志」页查看 |
| 对话能力说明 | 「使用全部服务商」时明确提示：各按其默认对话模型主备切换 |

验证：`flutter analyze` 0 error；`flutter test` 107/107（新增错误详情提取、模型 fallback 3 用例）；`flutter build windows --debug` ✅。

## 对话主模型明确选择 + AI 助手模型切换（2026-08-21）

用户反馈：对话（通用主模型）缺模型名选择造成混乱；AI 助手界面应显示当前模型并可切换。

| 项 | 说明 |
| :-- | :-- |
| 功能模型页 | 对话卡**单选服务商时显示模型下拉**（该服务商勾选的官方模型）；「全部」模式明确说明各按其默认对话模型主备切换 |
| 运行时 | `AiCompositeProvider` 支持指定对话主模型（服务商 + 模型名覆盖），其余启用服务商仍作备用；暴露 `chatLabel` 与 `chatModels` 可用列表 |
| AI 助手页 | 上下文条新增**模型 chip**（显示当前「服务商 · 模型」），点击弹出模型切换面板：全部（主备）/ 各服务商可用模型，选择即落盘（对话能力配置）并即时生效 |

验证：`flutter analyze` 0 error；`flutter test` 108/108（新增指定主模型与可用列表用例）；`flutter build windows --debug` ✅。

## AI 助手多会话 + 内容落地 + 对话提速（2026-08-21）

| 需求 | 实现 |
| :-- | :-- |
| 历史话题/新开话题 | Drift v5→v6 新增 `AiChatSessions`/`AiChatMessages` 表；AI 助手 AppBar「历史」弹会话列表（标题/时间/删除，点选切换）、「新开话题」按钮；首条消息自动生成会话标题；消息落库 |
| 内容落地 | 助手消息新增「存入笔记」「加入知识库」：存为新 Diary + text Block（双模态一致，标签 AI），可选跳详情；加入知识库 = 存笔记 + 索引进所选知识库 |
| 对话提速 | `AiProviderFactory` 全局缓存 Provider（复用 Dio/连接池，避免每次发送重建连接/TLS 握手）；历史消息渲染缓存（流式期间不重渲染已生成消息）；RAG 检索失败自动降级 |

慢因分析（结论见最终答复）：主因是每次发送重建 Dio 连接 + 流式期间全列表 Markdown 重渲染；已修复。

验证：`flutter analyze` 0 error；`flutter test` 110/110；`flutter build windows --debug` ✅。

## Phase 2 收口：P2.4/P2.5/P2.6 + P2.2 待办聚合（2026-08-21）

分支：`feat/P2.7-smart-canvas`（延续）。语音识别（P2.7.6）按用户指示继续后置。

| 任务 | 状态 | 说明 |
| :-- | :-- | :-- |
| P2.4 CRM 自定义表格 | ✅ | 业务对象页列表/表格双视图：表头点击排序、搜索过滤、列设置弹窗（按对象类型持久化，PrefUtil 新增前缀白名单 `crmTableColumns_`） |
| P2.5 CRM 业务下钻 | ✅ | 实体详情对话框内按名称搜索相关日记（`searchDiariesByText`），点击跳转日记详情 |
| P2.6 CRM FAB 快捷新建客户 | ✅ | 首页「新增客户」底部面板：日记搜索预填名称（AI 实体填充）+ 备注，保存走 CrmSyncService 同步 Twenty |
| P2.2 待办与日程聚合 | ✅ | 新增 `lib/features/todo` 模块：`TodoItem` 统一模型 + `TodoParser` 纯函数（Markdown 行/JSON 内容、CRM task 快照）+ `TodoAggregator`（合并排序/勾选落库/快捷新建）；`BlockMeta` 增 `dueDate`（走 metaJson 不改表）；日历页「时间轴/待办」切换 + 待办列表（本地勾选、跳日记详情、CRM 任务快照）+ 快捷新建入口（3 步内） |

### 验证快照（2026-08-21 Phase 2 收口）

- `flutter analyze`：0 error（仅 1 条既有 CorePalette deprecation info）
- `flutter test --exclude-tags integration`：121/121 全绿（新增 todo 9 用例）
- `flutter build windows --debug` ✅

### 遗留（后续轮次）

- P2.7.6 语音识别真实接入（用户明确搁置）
- P3.x RAG 的 LanceDB 原生接入（当前 Dart/Drift 向量表 + Rust LocalVectorIndex 均已可用，trait 依赖倒置可平滑替换）
- Phase 4：性能优化/冲突 UI/健康度工具/导入导出/扩展点（P4.1~P4.5）

## Phase 4 第一批：P4.4 导入导出 + P4.5 扩展点 + P4.1/P4.3 审计（2026-08-21）

| 任务 | 状态 | 说明 |
| :-- | :-- | :-- |
| P4.4 全量导入导出 | ✅ | `lib/features/backup/backup_service.dart`：zip 备份包含 manifest + diaries（JSON+Markdown 投影）+ categories + CRM + 知识库 + 向量 + AI 会话/消息；导入按 id 幂等 upsert、schema 版本校验；备份/同步页新增「导出/导入 Markdown+JSON 备份」入口（桌面选目录、移动端分享）；3 用例（往返一致/幂等/缺文件报错） |
| P4.5 扩展点完善 | ✅ | 架构文档新增「九、扩展点完善」：路由注册 / 卡片动作路由 / Block 协议 / AI 能力 / 待办聚合 / 数据备份 / 模块开关 / 主题设置共 8 处扩展点说明 |
| P4.1 性能优化 | ✅（审计） | 自有页面列表均为 builder 虚拟化（ListView.builder / ScrollablePositionedList），无一次性 `ListView(children:)` 热点 |
| P4.3 数据健康度 | ✅（既有能力汇总） | 附件孤立扫描清理（设置）、每知识库「索引全部卡片」（含重索引入口）、CRM 全量对账（远端缺失/本地过期差异报告）均已就位 |
| P4.2 冲突解决 UI | ➖（LWW 覆盖） | 同步引擎为 Last-Write-Wins，冲突由 reconcile 对账报告呈现；无独立冲突源，暂不引入专页 |

### 验证快照（Phase 4 第一批）

- `flutter analyze`：0 error（仅 1 条既有 CorePalette deprecation info）
- `flutter test --exclude-tags integration`：124/124 全绿（新增备份 3 用例）
- `flutter build windows --debug` ✅

### 遗留（后续轮次）

- P2.7.6 语音识别真实接入（用户明确搁置）
- P3.x RAG 的 LanceDB 原生接入（依赖倒置已就绪，按需替换）
- P4.2 冲突专页（若未来引入非 LWW 同步源再补）

## P4.3 数据健康度工具页（2026-08-22）

| 项 | 说明 |
| :-- | :-- |
| 入口 | 设置 → 数据 → 数据健康度（`AppRoutes.healthPage`） |
| 数据概览 | 日记 / Block / CRM 实体 / 知识库 / 向量索引 / 数据库大小，一键刷新（`HealthService.loadStats`） |
| 附件健康 | 扫描孤立附件（`AttachmentManager.scanOrphans`）→ 一键清理，显示数量 |
| 向量索引 | 每知识库重建入口升级为「重建全部向量索引」：`RagService.reindexAll` 遍历全部知识库，单库失败不中断，返回 (库数, 成功数, 索引块数) |
| 同步对账 | CRM 全量对账（读 Twenty 安全存储配置，未配置给指引），显示缺失/过期差异 |
| 测试 | `flutter test` 128/128（新增 loadStats 计数、reindexAll 全库索引 2 用例）；analyze 0 error；Windows 构建 ✅ |

### 里程碑状态

- M2（Phase 2）✅、M3（Phase 3）✅：均已演示验证
- M4（Phase 4）：P4.1 审计 ✅ / P4.3 ✅ / P4.4 ✅ / P4.5 ✅；P4.2 由 LWW + 对账覆盖，暂不单独建页
- 遗留：语音识别（P2.7.6，用户搁置）、LanceDB 原生接入（RAG 已可用，依赖倒置就绪）、合并 develop 收口

## 下一阶段：Twenty 数据对象对接 + CRM 顶部 Tab 智能表格（2026-08-22）

用户需求：按最初开发思想，在 Twenty 创建数据对象承接笔记/待办/跟进记录；
有客户关联的数据自动进入客户时间线（note/task + target），无关联数据落通用数据表
（后续可编辑、认领关联）；CRM 模块改为顶部 Tab 分页智能表格（客户/联系人/机会/合同等），
保留 Twenty 动态对应并可开关页面，支持增删改查。

| 任务 | 状态 | 备注 |
| :-- | :-- | :-- |
| P5.1 内容同步映射表 | ✅ | Drift schema v7：crm_content_links（localType/localId ↔ remoteType/remoteId + target + status） |
| P5.2 TwentyApiClient 内容 CRUD | ✅ | createNote/updateNote/createTask/updateTask/createNoteTarget/createTaskTarget + introspection typeExists |
| P5.3 CrmContentSyncService | ✅ | 笔记→note、待办→task；自动匹配（公司>联系人>机会）；无关联→moodiaryGeneric（降级无 target 标准对象）；认领/取消关联/拉取/删除 |
| P5.4 对接指导文档 | ✅ | docs/twenty-数据对象对接指导.md（对象字段表 + UI/GraphQL 创建命令 + 对接要求） |
| P5.5 CRM 顶部 Tab 智能表格 | ✅ | crm_home_page 重构：客户/联系人/机会/合同/回款/发票/提成/任务/笔记；pluto_grid 智能表格（排序/筛选/列宽/冻结/双击编辑）+ 增删改查 + 列设置持久化 |
| P5.6 页面显隐开关 | ✅ | 设置 → CRM 同步 → 页面显示管理（crmTabVisible_* 持久化） |
| P5.7 内容同步页 | ✅ | 一键推送 / 未关联认领 / 取消关联 / 删除远端 / 拉取 Twenty 内容 |
| P5.8 测试门禁 | ✅ | analyze 0 error；flutter test 138 通过（新增 10 例内容同步测试）；Windows 构建 ✅ |
| P5.9 真实环境验证 | ✅ | 新 API Key 已配置（config/twenty.local.json，不入 git）；连通/全量拉取/建删闭环 + 内容同步全链路集成测试通过；moodiaryGeneric 已在 Twenty 创建 |

### 架构说明

1. **对象映射**：Diary → Twenty `note`（title + bodyV2.markdown）；Todo Block → Twenty `task`
   （title/bodyV2/dueAt/status）；客户关联通过 `noteTarget`/`taskTarget` 建立，进入客户时间线；
   无关联数据写入自定义对象 `moodiaryGeneric`（通用数据表），认领后升级为标准对象并挂 target。
2. **幂等**：crm_content_links 唯一键（localType, localId），重复推送走更新不重复建对象。
3. **智能表格**：pluto_grid 8.1（成熟表格组件），列从实体快照动态发现 + 列设置持久化；
   单元格编辑防抖保存；新增/编辑/删除直接推 Twenty 并同步本地缓存。
4. **集成测试**：新增 dart_test.yaml，integration 标签默认跳过（需真实 Twenty 环境），
   联调命令 `flutter test --tags integration --run-skipped`。

### 遗留与下一步

- 语音识别（用户搁置）、LanceDB 原生接入、P4.2 冲突专页、全量同步携带媒体（不变）。
- 反向同步：Twenty 侧手改内容覆盖本地（当前内容推送以本地为准，LWW）。

### 真实环境验证记录（2026-08-22 二次验证）

用户提供新 API Key 后完成：

| 项 | 结果 |
| :-- | :-- |
| 连通性 | ✅ healthz OK；公司 7 / 联系人 5 / 商机 6 / 任务 0 |
| 全量拉取 + 建删闭环 | ✅ `twenty_integration_test` 通过（建公司→缓存搜索→删除） |
| 内容同步全链路 | ✅ `twenty_content_integration_test` 通过：关联笔记→note+noteTarget；关联待办→task+taskTarget；无关联→moodiaryGeneric；认领→升级 note+target 并删除通用记录；远端拉取可见 |
| 清理 | ✅ 测试数据全部清除（companies=7 / notes=0 / tasks=0 / moodiaryGenerics=0 / targets=0） |
| 对象创建 | ✅ 已在 Twenty 创建 `moodiaryGeneric`（title/content/sourceType(NOTE/TODO)/dueAt/status） |

联调中发现并修复 3 处真实环境差异：
1. noteTarget/taskTarget 的关联字段名为 `targetCompanyId/targetPersonId/targetOpportunityId`（非 companyId）；
2. 自定义对象在标准 API 的 `__type` 类型名为首字母大写 `MoodiaryGeneric`，`typeExists` 同时探测两种写法；
3. Twenty SELECT 枚举值必须大写蛇形（sourceType 用 NOTE/TODO），metadata 端点为本实例 `/metadata`。

## 打磨细节（2026-08-22 第三轮）

### 6.1 通用记录同步修复

**问题**：Twenty 通用记录表看不到记录（或记录显示为空白）。

**根因**：`moodiaryGeneric` 创建时未指定标签标识字段，Twenty 自动生成 `name` 作为
labelIdentifier；应用此前只写 `title`，记录在 Twenty 表格中显示为空。

**修复**（已合入 + 真实环境验证）：

| 项 | 说明 |
| :-- | :-- |
| 标签字段 | 通用记录创建/更新同时写入 `name`（=标题）与 `title`/`content`，保证 Twenty 表格可见 |
| 遗留迁移 | `pushAll` 自动重推「状态 generic 但远端为标准 note/task」的旧降级记录，迁移到通用表 |
| 强制重推 | 内容同步页新增「强制重推」按钮（force=true，全部按最新内容更新远端） |
| 清理 | 删除历史空标签通用记录；集成测试断言通用记录 `name` = 日记标题 |

### 6.2 CRM 表格字段：按 Twenty 规则默认展示 + 自定义

实现 [crm_field_registry.dart](lib/features/crm/crm_field_registry.dart)：

| 项 | 说明 |
| :-- | :-- |
| 元数据拉取 | `metadataGraphql` 拉取对象/字段（name/label/type/isSystem），缓存到 PrefUtil |
| 对象映射 | 应用对象 key ↔ Twenty metadata nameSingular（contract/payment/invoice/commission 等） |
| 默认列 | 标签标识字段（labelIdentifier）+ 可展示标量字段，按 metadata 顺序，排除系统/审计/关系字段 |
| 列设置 | 弹窗支持勾选显隐 + 拖拽排序 + 「恢复默认」；表格内直接拖列头排序并持久化 |
| 持久化 | `crmTableColumns_<对象>`（顺序）+ `crmTableColumnsCustomized_<对象>`（是否自定义） |

验证：单测 16（内容同步 + 字段注册表）；真实集成 2 条通过；analyze 0 error；全量 flutter test 144 通过。

### 6.3 第二轮打磨（表格单列 + 通用表仍空）

**问题 1：CRM Tab 表格只显示名称一列**

根因：`pullObject` 只按默认 `['id','name']` 拉取，快照没有其他字段可展示。

修复：`pullObject` 按 Twenty 元数据拉取完整可展示字段（标签字段 + 标量字段），
字段与 GraphQL 不一致时自动回退标签字段并记日志；`isDisplayField` 只排除审计字段
（dueAt 等可展示）。重新全量同步后表格即按 Twenty 默认字段展示多列。

**问题 2：Twenty 通用表仍无内容**

根因：应用内安全存储未保存新令牌（旧 Key 已吊销），推送全部失败；另通用记录
labelIdentifier（name）此前未写入。

修复：CRM 同步页新增「导入本地配置」（从 config/twenty.local.json 预填并保存）；
安全存储无令牌时自动预填本地配置；内容同步页无令牌时显示配置引导；通用记录
创建/更新写入 name（=标题）。历史降级记录由「强制重推」迁移到通用表。

**补充（P6.2）**：CRM 顶部 Tab 新增「通用记录」（moodiaryGeneric），Twenty 通用表
内容可回拉展示；fullPull 单对象失败不阻断（兼容未建通用表的实例）；字段注册表
补充 Twenty metadata 偶发缺失的 title/sourceType/status 字段。

演示数据（留在 Twenty，可随意清除）：笔记/待办挂 Notion 客户时间线（客户拜访纪要、
跟进合同报价）+ 2 条通用记录（灵感速记、整理清单）。

### 6.4 关键 CRM 字段补齐（GraphQL schema 权威）

**问题**：表格已多字段，但 Twenty 预设的关键 CRM 字段（域名/员工数/年经常性收入/
客户状态/职位/城市/金额/阶段等）仍缺失。

**根因**：Twenty metadata `fields` 连接存在缓存怪癖，字段列表不完整
（公司仅返回 name/address/xLink/linkedinLink 等 6 个），GraphQL schema 才是权威。

**修复**：

| 项 | 说明 |
| :-- | :-- |
| 权威字段源 | `TwentyApiClient.typeFields` 通过 introspection 拉取 GraphQL 字段（名称/类型/kind） |
| 合并 | CrmFieldRegistry 合并 schema 字段（权威）+ metadata 标签；ENUM→SELECT；复合标量（Links/Address/Currency/Emails/Phones/FullName）可展示，关系对象（Connection/实体类型）排除 |
| 客户表 | 名称/域名/地址/员工数/领英/X/年经常性收入/客户状态（预设+自定义）等 |
| 联系人表 | 姓名/邮箱/电话/职位/城市/头像/微信/领英/X |
| 机会表 | 名称/金额/预计成交日/阶段/商机状态 |
| 自定义 | 以上全部进入「列设置」，可显隐 + 拖拽排序 + 恢复默认 |

验证：analyze 0 error；单测 145；真实集成断言公司快照含 ≥3 个关键字段通过。

### 7 结构同步与数据同步分离（2026-08-22）

按用户要求把两类同步彻底分开，设置页三类入口清晰：

| 类别 | 说明 | 触发方式 |
| :-- | :-- | :-- |
| 数据结构同步 | 对象/字段元数据（metadata + GraphQL schema 合并），结果缓存到本地（crmFieldMeta_v2_* + 版本/时间） | 初始化、结构版本升级时自动；手动按钮 |
| 数据记录同步 | 客户/联系人/机会等记录数据（fullPull） | 日常手动/定时 |
| 全量同步 | 结构 + 数据一起 | 手动按钮 / CRM 首页「全量同步」 |

要点：
- `CrmStructureSyncService`：结构版本（structureVersion）+ 上次同步时间持久化；
  `ensureSynced()` 仅在版本落后或缓存缺失时执行，之后数据同步零 metadata 网络开销；
- `CrmFieldRegistry.fetchObjectMeta` 默认读缓存（内存→PrefUtil），`force` 才联网刷新；
- 设置 → CRM 同步页同步卡片重构：测试连接 / 结构同步 / 数据同步 / 全量同步 / 对账，
  每类独立按钮与状态（上次时间、对象数、字段数、记录条数）；
- CRM 首页初始化后台自动补结构；「全量同步」= 结构 + 数据；
- 集成测试扩展：联系人（职位/城市/微信/邮箱/电话）、机会（金额/预计成交日/阶段）关键字段断言。

验证：analyze 0 error；单测 146；真实集成 2 条通过。

### 8 联系人表排查：复合字段可读化（2026-08-22）

**现象**：联系人表字段不全、名称列显示为 Twenty ID、关键字段为空。

**根因（通用问题）**：
1. `person.name` 是 FullName 复合类型，测试数据为 null → 缓存名称回退成记录 ID；
2. emails/phones/Links/Address/Currency/Actor 等复合字段在 GraphQL 中是对象，
   直接 `toString()` 不可读（影响公司域名/ARR、机会金额等所有对象）；
3. 默认列未对齐 Twenty：缺 公司（关系）、创建时间、创建人。

**修复**：

| 项 | 说明 |
| :-- | :-- |
| formatValue | Map 复合值→单行文本：FullName 组合、primaryEmail、primaryPhoneNumber、primaryLinkUrl、Currency 金额、Actor 名称、关系 name；ISO 日期→yyyy-MM-dd |
| 名称提取 | `_nameOf` 复合感知 + 非空兜底（jobTitle/emails/phones/city），不再以 UUID 作为主名称 |
| 默认列对齐 Twenty | 联系人：姓名/职位/公司/电话/邮箱/城市/创建时间/领英/创建人/X；机会：公司/联系人/负责人；任务：负责人；公司：负责人 |
| 关系标签列 | 以 `field { name }` 拉取关联对象名称（company/pointOfContact/owner/assignee/accountOwner/heTong），单元格只读 |
| 复合/关系列只读 | 避免在表格内编辑破坏结构化数据（编辑走详情表单） |

演示：新增字段完整联系人（Ada Lovelace / Notion / 邮箱 / 电话 / 上海）。
验证：analyze 0 error；单测 148；真实集成（联系人名称≠ID、公司列可读）通过。

## 9 本地化 CRM 转型（2026-08-22，分支 feat/local-crm-v1）

**决策（用户拍板）**：完全放弃 Twenty 作为 CRM 真相源，参照
SuiteCRM/EspoCRM/Twenty 的成熟数据模型，在本地 Drift 直接实现 CRM；
基础对象首批：公司/联系人/机会（线索）/合同；后期通过自定义数据对象引擎
扩展回款/发票等。完整讨论见 docs/架构讨论-本地化CRM方案.md。

### 9.1 已完成（本分支第一步：数据层 + 首页本地化）

| 项 | 说明 |
| :-- | :-- |
| schema v8 | CrmCompanies / CrmPeople / CrmOpportunities / CrmContracts 强类型表 + CrmObjectDefs/CrmCustomRecords（自定义对象引擎）+ CrmEntityLinks（实体↔日记/待办关联） |
| CrmLocalRepository | 基础对象 CRUD/搜索、自定义对象定义与记录、关联、统计（lib/features/crm/local/） |
| 字段定义 | kBaseObjectFields 本地字段默认列（对齐成熟 CRM 视图），复用列设置/显隐/排序 |
| CRM 首页 | 纯本地：基础 Tab + 自定义对象 Tab，增删改查走仓储层，无网络依赖 |
| 设置页 | 移除 Twenty 同步入口，改为「CRM（本地）」状态展示 |
| 测试 | 仓储层 5 用例 + 迁移 v8 断言更新；全量 153 通过；analyze 0 error；Windows 构建 ✅ |

### 9.2 下一步（待续）

- 自定义对象管理 UI（新建/编辑对象定义与字段）；
- 回款/发票作为自定义对象示例落地；
- 实体详情页（公司→联系人/机会/合同下钻 + 时间线）；
- 跟进记录/认领全本地化（CrmEntityLinks + BlockMeta.entityId）；
- 导入导出：把本地 CRM 纳入备份格式；Twenty 数据可选一次性导入；
- 清理 Twenty 遗留代码（CrmSyncService/CrmFieldRegistry 等收拢或删除）。

> 分阶段计划见 [CRM分阶段开发计划.md](docs/CRM分阶段开发计划.md)
> （S0–S6：核心对象 → 销售流程 → 财务回款 → 售后 → 通用支撑 → 体验生态）。

### 9.3 S0/S1 首批落地（2026-08-22，分支 feat/local-crm-v1）

用户确认 R0/R1 决策后完成：

| 项 | 说明 |
| :-- | :-- |
| schema v9 | Account 统一客户模型（company/person/org）+ Contact 从属；Opportunity 按 19 表文档重建（stage 8 阶段/概率/金额/线索暂存/成交日期/输单原因）；Contract 按文档重建（单号/冗余 paid/invoiced/起止/质保到期） |
| 模型/仓储 | LocalAccount/LocalContact 替代 Company/Person；机会/合同新字段 CRUD/搜索/软删 |
| UI | Tab 切换为 客户/联系人/机会线索/合同；智能表格/列设置/CRUD 走新仓储 |
| S0.2 | 同步日志落盘修复：启动时 attach 到应用支持目录 logs/sync.log（此前只存内存） |
| 测试 | 仓储 6 用例 + 迁移 v9 断言；全量 154 通过；analyze 0 error；Windows 构建 ✅ |

### 9.4 S1.5 + S2 落地（2026-08-22，分支 feat/local-crm-v1）

**S1.5 实体详情页**：

| 项 | 说明 |
| :-- | :-- |
| CrmEntityDetailPage | 双击表格行进入：字段展示（读标签）、客户下钻（联系人/机会/合同）、机会下钻（报价/合同）、相关日记（CrmEntityLinks + 名称搜索兜底）、删除入口 |
| 报价明细 | 报价详情内添加/删除明细（产品选择/数量/单价/折扣），合计自动重算 |
| 转合同 | accepted 报价一键转合同（生成 HT 单号 + 明细快照，幂等） |

**S2 产品与销售流程（schema v10）**：

| 表 | 说明 |
| :-- | :-- |
| CrmProductCategories / CrmProducts | 分类（两级）+ 产品/服务（SKU 唯一、单价/成本、质保月数、在售） |
| CrmQuotes / CrmQuoteItems | 报价单（QT-YYYYMMDD-XXX 自动编号、状态流转、合计）+ 明细（产品名快照、数量/单价/折扣/小计） |
| CrmContractItems | 合同明细（产品名快照、质保月数），报价转合同自动生成 |
| 单号生成 | `_nextSequenceNo`：前缀 + 日期 + 当日自增 3 位（QT/HT，AS 预留） |
| 报价转合同 | `quoteToContract`：accepted 校验 + 幂等（已转换返回原合同）+ 明细快照 + 合同金额汇总 |

验证：analyze 0 error；单测 157（新增产品 SKU/报价合计/转合同幂等 3 例）；Windows 构建 ✅。

### 9.5 S3 财务回款（2026-08-22，分支 feat/local-crm-v1）

**schema v11 + 仓储**：

| 表 | 说明 |
| :-- | :-- |
| CrmPaymentPlans | 回款计划（期次/计划金额/已收/计划日期/状态 pending·partial·completed·overdue） |
| CrmPayments | 回款记录（金额/日期/方式 cash·transfer·check·wechat·alipay/关联计划/发票/备注） |
| CrmInvoices | 发票（号/类型 vat_special·vat_normal·electronic/金额/税率/状态/收票人） |

**关键机制**：
- 冗余维护：新增/删除回款自动累加 `Contract.paidAmount`；新增/删除发票（非作废）自动累加 `Contract.invoicedAmount`；回款同时回写 `PaymentPlan.paidAmount` 与状态（completed/partial/overdue 自动判定）；
- 到期提醒 `dueReminders()`：7 天内未完成回款计划 + 30 天内到期合同聚合；
- Tab 新增 回款计划/回款/发票；合同详情页新增财务下钻（计划/记录/发票 + 已回款/已开票汇总）。

验证：analyze 0 error；单测 159（新增冗余维护/计划状态/提醒聚合 2 例）；Windows 构建 ✅。

### 9.6 S4 售后服务（2026-08-22，分支 feat/local-crm-v1）

**schema v12 + 仓储**：

| 表 | 说明 |
| :-- | :-- |
| CrmWarranties | 质保（序列号/起止/状态 active·expired·void），到期自动置 expired |
| CrmAfterSales | 售后工单（AS-YYYYMMDD-XXX 自动编号、类型/优先级/状态流转、解决/关闭时间自动落） |

- 质保到期进入 `dueReminders()`（30 天内）；
- Tab 新增 质保/售后；合同详情页新增质保列表；
- 单号生成器扩展 AS 前缀。

验证：analyze 0 error；单测 160（新增质保过期/AS 编号/质保提醒 1 例）；Windows 构建 ✅。

**至此 M-CRM1/M-CRM2/M-CRM3 里程碑对应能力已具备**（核心对象、销售流程、财务、售后），
下一步 S5 通用支撑（Activity 跟进/时间线、标签、附件、提醒通知、日历联动）。

### 9.7 演示数据 + S5 首批（2026-08-23，分支 feat/local-crm-v1）

**演示数据**：`CrmDemoData.seed` 为全部表生成 5–10 条真实关联数据
（产品分类/产品 8、客户 6、联系人 8、机会 7、报价 6+明细 12、合同 6+明细、回款计划/回款/发票各 6、
质保/售后各 6、自定义对象「拜访记录」1 定义 + 6 记录），并验证：
报价转合同、合同冗余金额与回款/发票一致、单号规则、自定义对象引擎。
CRM 首页新增「✨」入口一键生成（追加不删除）。

**S5.1/S5.2/S5.3（schema v13）**：

| 项 | 说明 |
| :-- | :-- |
| CrmActivities | 跟进记录（多态 relatedType/relatedId，类型 call·meeting·email·wechat·visit·task·note，计划/完成/取消） |
| 时间线 | 实体详情页合并「跟进 + 相关日记」按时间倒序 |
| CrmTags/CrmEntityTags | 标签（名称唯一）+ 任意实体打标（差异同步增删），详情页标签 chips |
| 快速跟进 | 实体详情页一键新增跟进（电话/会议/拜访…） |
| Tab | 新增 跟进 |

验证：analyze 0 error；单测 163（演示数据 2 例 + 跟进/标签 1 例）；Windows 构建 ✅。

**S5 剩余**：Attachment 附件沙盒、Reminder 本地通知、日历联动（下一步）。

### 9.9 S6.1 数据看板 + UI 交互升级（2026-08-23，分支 feat/local-crm-v1）

**UI 交互升级（Twenty 式）**：

| 项 | 说明 |
| :-- | :-- |
| 桌面右侧面板 | 宽屏（≥900px）双击行 → 右侧 420px 详情/编辑一体面板（替代弹窗）；表格左侧保留 |
| 移动端 | 窄屏仍整页详情 |
| 原位编辑 | 字段行点击即编辑：文本回车保存、下拉即存（快速保存），全对象复用 CrmEntityDetailView |
| 统一更新/删除 | CrmEntityFieldUpdater / CrmEntityDeleter 覆盖全部 14 类对象 |

**S6.1 数据看板**（CRM 首页「看板」入口）：

| 区块 | 内容 |
| :-- | :-- |
| 对象统计 | 客户/联系人/机会/合同/产品/报价 计数 |
| 机会漏斗 | 8 阶段数量 + 赢单金额 / 在谈管线 |
| 回款趋势 | 近 6 个月回款条形图 |
| 近期到期 | dueReminders 列表（回款/合同/质保） |

验证：analyze 0 error；单测 164；Windows 构建 ✅。

**S6 剩余**：AI 辅助录入（语音/文本提取实体字段）、PDF 导出（报价/合同/发票）、
本地 CRM 纳入备份导入导出、多设备同步预留设计（下一步）。

### 9.10 S6 主体完成：AI 提取 / PDF / 备份 / 多设备预留（2026-08-23）

| 项 | 说明 |
| :-- | :-- |
| 右侧栏交互修复 | 原位编辑失焦自动提交（FocusNode 监听）+ 防重复保存 + 提交后刷新显示 |
| S6.2 AI 辅助录入 | `CrmAiAssist.extractFromText` 启发式提取（名称/金额(万/元换算)/电话/邮箱/日期/阶段），新增对话框「AI 填充」粘贴文本自动填表；接口稳定，后续可换 LLM 抽取（语音暂缓） |
| S6.3 PDF 导出 | `CrmPdfExport` 报价/合同/发票生成 A4 PDF（含明细表/合计/金额冗余），保存 `Exports/CRM/` 并系统打开；详情面板「导出 PDF」 |
| S6.4 备份纳入 | `CrmBackupCodec` 全量导出/导入 20+ 本地 CRM 实体（幂等合并），备份包新增 `crm_local.json`，导入恢复 |
| S6.5 多设备预留 | 设计要点：CrmEntityLinks/Activity 纳入 Rust 同步 DTO；本地时间戳 + 软删墓碑；CRDT 评估（字段级 LWW 已满足单人场景，多设备用「设备+时间」排序 + 对账） |

验证：analyze 0 error；单测 168（新增 AI 提取 2 + PDF 3 文档字节 + 备份往返 1）；Windows 构建 ✅。

### 9.11 CRM UI 大范围优化：Twenty 风格内联化（2026-08-23）

**1. 修复列设置保存后不立即刷新**：先落盘再 setState（此前 setState 先于 PrefUtil 写入，
重建时读到旧的自定义标记，需切页才生效）。

**2. 少用弹窗，向 Twenty 风格靠拢**：

| 交互 | 之前 | 现在 |
| :-- | :-- | :-- |
| 列设置 | AlertDialog | 桌面右侧内联面板（CrmColumnSettingsPanel：拖拽排序/勾选/隐藏 chips/恢复默认/保存），移动端保留弹窗 |
| 新增 | AlertDialog | 桌面右侧内联表单面板（CrmCreateFormPanel：字段内联 + AI 填充内联输入区），移动端保留弹窗 |
| 标签添加 | AlertDialog | 标签卡内联输入（输入框 + 确认/取消） |
| 快速跟进 | AlertDialog | 详情/面板内联表单（主题 + 类型/状态下拉 + 保存） |
| 详情/编辑 | 弹窗/整页 | 桌面右侧详情/编辑一体面板（此前已做），字段原位编辑失焦自动保存 |

验证：analyze 0 error；单测 168；Windows 构建 ✅。

### 9.13 新建即时刷新 + 字段类型/输入约束 + 报价模板/版本（2026-08-23）

**1. 新建记录即时刷新**：创建完成 → 先关闭面板 → 再 `_refreshGrid()` 单一路径，
保证网格立即出现新行（此前顺序导致偶发不刷新）。

**2. 字段类型与输入约束（全模块扫查）**：

| 类型 | 输入方式 |
| :-- | :-- |
| select | 新增面板/详情原位编辑均为下拉 + 「＋添加选项」（内联输入新选项并应用） |
| date | 可输入（yyyy-MM-dd）+ 日历选择器（新增面板与详情编辑都有） |
| currency | 下拉选择常用币种（CNY/USD/EUR/GBP/HKD/JPY），默认 CNY，可加自定义 |
| number | 数字键盘；text/textarea 文本 |
| 表格单元格 | select/currency 字段单元格改为下拉（pluto_grid select 列） |

金额类字段统一标记为 currency（机会金额/合同总金额/已回款/已开票/单价/成本/报价合计/
折扣/计划/回款/发票金额等）。

**3. 报价模板 + 快速报价 + 版本保存（schema v15）**：
- `CrmQuoteTemplateStore`：保存当前报价为模板（名称/默认有效期/明细），从模板一键生成新报价单（草稿+明细+有效期）；
- `CrmQuoteVersions`：报价版本快照表——保存当前版本（V1/V2…）、列出历史版本、恢复任意版本（覆盖报价字段+明细并重算合计）、删除版本；
- 报价详情操作区新增：存模板 / 模板报价 / 版本。

验证：analyze 0 error；单测 169（新增报价版本保存/恢复 1 例）；Windows 构建 ✅。

### 9.14 货币复合输入 + 日期输入优化（2026-08-23，schema v16）

**1. 货币字段 = 复合组件「币种下拉 + 金额输入」**：
- 新增面板：金额类字段显示为 `[币种下拉][金额输入]`，币种可改（默认 CNY，可加自定义）、金额手动输入；
- schema v16：合同/回款/发票/报价/产品 增加 `currency` 列，币种选择真正落库；
- 机会已有 currency 列；其余实体经 `<字段>Currency` 映射写入实体 currency；
- 表格单元格：金额列恢复数字输入，仅「币种」列用下拉。

**2. 日期输入优化**：
- 日历选择图标**内嵌在日期编辑框内**（suffixIcon），不再因点击图标失焦丢编辑；
- 选择日期后**自动保存**；失焦/回车同样自动保存；
- 空值显示灰色「未设置日期」占位；
- 输入归一化：`normalizeDateInput` 支持 `yyyy-MM-dd`、`yyyy/M/d`、`yyyy年M月d日`、`yyyy.M.d`，统一存 `yyyy-MM-dd`。

验证：analyze 0 error；单测 169；Windows 构建（本轮因 App 运行中未覆盖，前轮已通过）。

### 9.15 货币单框复合输入 + 表单去重精简（2026-08-23）

**1. 金额输入统一为「币种+金额」单框复合组件**（CrmCurrencyAmountField）：
- 单个圆角边框内：左侧币种下拉 + 分隔线 + 右侧金额输入（Twenty 风格）；
- 新增面板与详情面板（右侧栏）都使用该组件；
- 表单中**不再出现独立的币种输入行**（表格里仍保留币种列供查看/修改）。

**2. 去重/精简扫查**：
- 新增表单隐藏自动计算字段（合同已收/已开票、回款计划已收），避免手工录入与流水不一致；
- 详情字段卡隐藏币种行（由金额复合组件承载）；
- 币种落库键统一映射（totalAmountCurrency/amountCurrency/planAmountCurrency/discountAmountCurrency/priceCurrency → 实体 currency）。

验证：analyze 0 error；单测 169；Windows 构建（App 运行中未覆盖，前轮已通过）。

### 9.16 关联交互 + 面板栈逐层返回（2026-08-23）

**1. 客户/机会/合同/报价详情内原位关联**（Twenty 风格）：
- 详情「关联业务」区右上 `⊕` 下拉（PopupMenu）：按类型分组——「新增联系人/机会/合同/报价…」与「关联已有 · 记录名」（仅列出未挂靠记录，避免误抢）；
- 关联已有：原位选择后立即 `CrmEntityLinker.link()` 落库（写父表外键），不跳转对应页面侧栏；
- 新增并关联：压入目标对象「新增」侧栏帧，创建成功**自动关联父实体**后逐层返回原详情；
- 已关联记录支持点击**下钻**进入其详情（面板栈再压一层），逐层关闭即回程。

**2. 右侧栏由单面板改为面板栈**（`_PanelFrame`：detail / columns / create）：
- `Offstage` 保留底层帧状态与滚动位置——返回时回到「客户中添加联系人的位置」；
- 关闭/点面板外仅弹出最上层面板并刷新网格；删除当前帧记录后同样弹回上一层；
- 移动端沿用整页导航栈：详情 → 新增页（自动关联）→ 返回刷新，等价逐层返回。

**3. 列表新增内联化**：
- 移除工具栏「新增」按钮，改为列表内容末尾「＋ 新增某某」操作行（Twenty 记录表样式）；
- 点击即打开该对象的侧栏新增帧；空列表也经同一入口创建。

**4. 代码复用**：
- 抽出 `CrmEntityCreator.createCrmEntity()`（表格/关联新增/移动端共用，返回新记录 id）；
- `CrmEntityLinker` 统一父子关联写入；`CrmCreatePage` 移动整页封装。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.17 关系字段全量可编辑（2026-08-23）

**背景**：relation 字段此前在详情字段卡与新增表单中被整体排除，导致联系人详情无法修改/新建"所属客户"，客户字段区也没有"联系人"等关系字段。

**1. 关系字段进入详情字段卡（右侧栏原位编辑）**：
- 客户详情新增「联系人 / 商机 / 合同 / 报价」关系字段（父侧，多值：显示已关联 n 条，下拉关联已有/新增）；
- 联系人/机会/合同/报价/回款计划/回款/发票/质保/售后等子侧关系字段（所属客户、签约联系人、商机、合同、回款计划、产品等）：点击原位下拉——选已有记录即关联、可「取消关联」、可「新增并挂到」（新建父实体后自动把当前实体挂上去，逐层返回）；
- 统一关系定义 `kRelationDefs`（对象 → 字段 → 候选类型 + 方向），`CrmRelationDef` 标记父侧/子侧。

**2. 新增表单支持关系选择**：
- 新增联系人可直接下拉选「所属客户」（含不关联）；新增机会/合同/报价/回款计划/回款/发票/质保/售后同理可选其关系字段；
- 保存时把关系选择写为外键（accountId/contactId/opportunityId/contractId/planId/productId/quoteId），新建即关联。

**3. 底层写入统一**：
- `CrmEntityLinker` 重写为按 targetType 驱动，覆盖全部关系方向（含父侧挂子、子侧挂父），空 parentId 表示解除关联；
- `CrmEntityFieldUpdater` 支持外键字段写入；`createCrmEntity` 各分支读取外键；
- 抽取共享 `crmRecordLabel/crmRecordId`（各对象主标签），详情与新增表单复用。

验证：analyze 0 error；单测 169 全绿；Windows 构建（App 运行中被 DLL 锁定未覆盖，编译阶段已通过，前轮已通过）。

### 9.18 关系字段显示修复：客户表关联列空白 + 父侧字段直接显示对象名称（2026-08-23）

**1. 客户表格关系列空白**：
- 根因：`_loadItems` 的 account 数据 map 未填充 `contact/opportunity/contract/quote` 键，默认列虽出现但值为空；
- 修复：account 加载时按 accountId 分组关联记录，关系列填充为 `{'name': '张三、李四'}` 等名称列表（≤3 个顿号连接，超过显示「前 3 个 等 n 个」），列保持只读；新增关联后刷新网格即时显示。

**2. 详情字段区父侧直接显示关联对象**：
- 客户详情「联系人/商机/合同/报价」不再显示「已关联 n 条」，改为直接显示对象名称列表（同表格规则），点击仍可下拉新增/关联已有；
- 新增外部刷新计数 `refreshTick`：详情/新增返回、关联保存后失效关系候选缓存并重载，保证「新增联系人 → 返回客户 → 立即显示」。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.19 数据结构规划指南 + CRM 设置页（功能开关/管理）（2026-08-24）

**1. 数据结构规划指南（权威文档）**：
- 新增 [CRM数据结构规划指南.md](docs/CRM数据结构规划指南.md)：综合《CRM 数据结构模型设计》与《CRM 双向无感关联设计指南》，按本项目技术栈（GetX + 统一仓储）定稿；
- **数据分类统筹**：L1 核心对象（Account/Contact）→ L2 交易对象（Opportunity/Quote/Contract）→ L3 财务对象（PaymentPlan/Payment/Invoice）→ L4 服务对象（Warranty/AfterSales）→ L5 支撑对象（Product/Activity/Tag/Attachment/Reminder）→ L6 扩展对象（元数据引擎）；
- 统一枚举表、公共字段规范（id/createdAt/updatedAt/deleted）、外键命名、币种规则、双向关联矩阵（单值/多值/多态）、级联规则、状态机、开发遵循清单。

**2. CRM 设置页（设计页 → CRM（本地）→ 本地 CRM 数据）**：
- **对象页签开关**：13 个内置对象逐个显示/隐藏（`crmTabVisible_<type>`），CRM 首页与业务对象页实时过滤；
- **通用**：默认币种下拉（`crmDefaultCurrency`，新建金额字段与复合组件生效）、恢复全部列设置为默认；
- **自定义对象管理**：列表展示（单复数名/字段数）、新建（含默认名称字段）、删除（级联删记录）；
- **数据**：生成演示数据入口。

**3. 开关生效点**：`CrmPrefs.enabledTabs()` 统一过滤 CRM 首页 Tab 与业务对象页；`CrmPrefs.defaultCurrency()` 用于新增表单初始化与金额组件回退。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅。

### 9.20 关联交互补齐（客户/联系人）+ 行内删除 + 设置数据管理 + 演示数据规范（2026-08-24）

**1. 客户/联系人关联字段缺失根因与修复**：
- 根因：客户（Account）是**父侧多值关系**（联系人/商机/合同/报价反向指向客户），其关系字段在新增表单被当作**子侧单值下拉**渲染（语义颠倒）；联系人的「所属客户」下拉只有选已有、没有「新建客户」入口；
- 修复：新增表单**过滤父侧关系字段**（由父详情管理）；子侧关系字段下拉增加「＋ 新建并关联」内联入口——输入名称即创建父记录并回填选中（不离开页面，符合 Twenty 双向无感规范）；
- 详情/表格数据路径不变（`_buildRelationTile` 全对象共用）。

**2. 表格行内删除**：
- `CrmSmartTable` 每行末尾新增操作列（renderer 删除按钮）→ 确认 → 软删除 → 网格即时刷新；详情面板删除保留。

**3. 设置页数据管理（测试调试）**：
- 一键清空 CRM 数据（`CrmLocalRepository.clearAllCrm` 事务物理删除全部记录，保留自定义对象定义）；
- 导出数据（JSON，`FilePicker.saveFile` + `CrmBackupCodec.exportAll`）；
- 导入数据（JSON，`importAll` 幂等合并）。

**4. 演示数据规范**：
- 新增跟进记录（8 条，多态关联客户/商机，类型/方向/状态/计划时间按枚举合规）；
- 标签（重点客户/VIP/大单/已签约，多态 N:M）；
- 提醒（回款/质保/跟进/合同到期 4 类，挂合同）；
- 原有数据保持客户→联系人→商机→报价→合同→回款/发票→质保/售后完整链路。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅。

### 9.21 新建后即时刷新 + 首页工具条重构 + 完善清单（2026-08-24）

**1. 新建/编辑返回后表格不刷新修复**：
- 根因：移动端/窄屏新建与详情走 `Get.to` 整页，返回后 Tab 页未重新加载数据；
- 修复：详情页与新增页 `Get.to(...).then(...)` 返回即 `_refreshGrid()`，客户/联系人/机会等新建后无需手动刷新。

**2. 首页标题行工具条（Twenty 式）**：
- 搜索框、列字段设置、刷新从各 Tab 内移到 CRM 标题行右侧（搜索框限宽、右侧对齐）；
- 新增 `CrmTableController`（query/refreshTick/columnsTick ValueNotifier）作为外部控制通道，仅当前激活 Tab 响应；
- 标题行保留数据看板按钮；演示数据按钮移除（设置页已有「生成演示数据」）；
- Tab 独立使用（业务对象页）时保留原内部工具条（controller 为空时显示）。

**3. 完善清单（待批准）**：
- 新增 [CRM完善清单.md](docs/CRM完善清单.md)：对照 Twenty/HubSpot/Salesforce 的 22 项不足与建议，分 P0 体验（搜索式关联/防重复/看板/全局搜索/批量）、P1 数据一致性（枚举统一/编号重试/审计/回收站）、P2 业务能力（客户360/合同履约联动/续约/CSV 导出）、P3 平台效率；批准后按排期实施。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅。

### 9.22 完善清单第一批：枚举统一 / 编号重试 / 候选分页 / CSV 导出（2026-08-24）

**1. 枚举统一（#6，商机阶段 key→label）**：
- 库存统一英文 key（newLead/contacted/qualified/proposal/negotiation/closedWon/closedLost/abandoned），UI 显示中文 label；
- `LocalObjectField` 增加 `optionLabels`（key→label），详情/新增/表格三处 select 渲染统一显示中文、存储英文；
- AI 提取、演示数据、数据看板同步改英文 key；搜索支持中文 label 匹配；
- 迁移 v17：存量中文 stage 一键转英文 key（`migration.dart`）。

**2. 编号冲突重试（#7）**：`_nextSequenceNo` 生成候选号前先查占用，被占用自动顺延（QT/HT/AS）。

**3. 关系候选分页（#10）**：新增表单关系下拉限最近 50 条（详情菜单已有 30 条上限），避免大数据量卡顿。

**4. 表格导出 CSV（#18）**：首页标题行新增导出按钮 → 当前对象按当前视图列导出 CSV（UTF-8 BOM 兼容 Excel，字段值/中文 label 正确转义）。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅。

### 9.23 完善清单第二批：搜索式关联 / 防重复 / 全局搜索 / 批量操作（2026-08-24）

**1. 搜索式关联（#1，Twenty RelationPicker）**：
- 新增 `CrmRelationSearchField` 组件：点击展开搜索（输入即搜 300ms 防抖）、空输入显示最近 5 条、无结果「+ 新建」内联极简表单、键盘 ↑↓/Enter/Esc；
- 详情字段卡与新增表单的关系字段统一替换为搜索式组件（子侧：选择/取消/新建即关联；父侧：未挂靠候选搜索、新建即挂靠）；
- 详情内联新建走 `_createRelationRecord`（创建即双向关联并即时刷新），新增表单内联新建返回新 id 回填外键。

**2. 新建防重复（#2）**：
- 仓储新增 `findDuplicateAccount/findDuplicateContact`（客户按名称、联系人按姓名+手机号）；
- 完整新增表单提交前查重 → 「仍然新建/取消」；
- 关联内联新建查重 → 「使用已有/仍然新建/取消」，使用已有直接回填已有记录。

**3. 全局搜索（#4）**：
- 首页搜索框输入 300ms 防抖跨 Account/Contact/Opportunity/Contract/Quote 搜索（仓储 keyword），浮层分组展示，点击跳转对应对象页并带入过滤词；
- TabController 改为 TickerProviderStateMixin + 延迟释放旧 controller（长度变化安全重建）。

**4. 批量操作（#5）**：
- 表格首列勾选框（多选）；选中后顶部操作条：批量删除（一次确认）、批量导出 CSV（仅选中行）、清除选择。

验证：analyze 0 error；单测 169 全绿；Windows 构建 ✅。

### 9.24 细节打磨：切 Tab 即时刷新 / 关联编辑失焦自动保存 / 表格关系列原位编辑（2026-08-24）

**1. 新建/修改后表格刷新再修复**：
- 根因补充：切 Tab 时目标表不刷新，跨表新建（如联系人页内联新建客户）后切到客户表显示旧数据；
- 修复：Tab 切换完成后（post-frame）触发目标表 `refreshTick++` 刷新；结合既有移动端返回刷新，全链路即时可见。

**2. 搜索式关联失焦自动保存**：
- 内联新建表单去掉「确认/取消」按钮：单输入框，回车或**失焦自动创建并关联**（名称非空），Esc 取消；
- 搜索框去掉关闭按钮，失焦自动收起（250ms 延迟避免吞结果点击）。

**3. 表格关系字段列原位编辑**：
- 表格关系列（如联系人的「所属客户」、机会的「客户/联系人」）点击单元格 → 底部弹出搜索式关联编辑（选已有/取消/新建即关联并刷新）；
- 与详情字段卡、新增表单共用 `CrmRelationSearchField`，全链路一致。

验证：analyze 0 error；单测 169 全绿；Windows 编译通过（App 运行中构建被 DLL 锁定，前轮已通过）。

### 9.25 关联保存链路加固（2026-08-24）

**根因排查**：新增 `crm_linker_flow_test.dart` 4 条端到端测试验证数据层全部正常（CrmEntityLinker 双向/解除、createCrmEntity 外键、CrmEntityFieldUpdater 外键写入），定位问题在 UI 交互层：
- 关联保存是"异步回调 + 立即回显"，选择/新建后界面先显示成功，实际写入失败时无感知；
- 内联新建路径存在**双重 link**（创建函数 link 一次 + onSelect 再 link 一次），且失败时界面假装已关联。

**修复**：
- `CrmRelationSearchField.onSelect` 改为返回 Future，**保存成功后才回显选中值**，失败保持编辑态并提示；
- 统一写入链路：`onCreate` 仅创建记录返回 id，关联统一由 `onSelect` 执行（详情/新增表单/表格三处一致），消除双重写入；
- 数据层回归测试入库（关联/外键/解除共 4 例）。

验证：analyze 0 error；单测 173 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.26 关联新建未落库修复（2026-08-24）

**根因（用户实测定位）**：客户详情 → 联系人字段 → 无结果新建 → 输入名称失焦，右侧栏"临时显示"姓名但退出后未保存。
- `CrmRelationSearchField` 中 `_creating` 同时充当「创建模式」和「提交中」两个标志；
- 点击「+ 新建」后 `_creating=true`，失焦/回车触发 `_submitCreate()` 时被开头 `if (... || _creating) return;` 直接拦截 → **创建与关联从未执行**（选择已有不受影响）。

**修复**：
- 拆分 `_creating`（创建模式）与 `_submitting`（提交中），`_submitCreate` 只防 `_submitting` 重入；
- 失焦/回车现在真正执行「创建 → onSelect 统一关联 → 回显」，退出右侧栏后关联持久化；
- 新增组件级回归测试（失焦自动创建并关联 / 选择已有即关联回显）。

验证：analyze 0 error；单测 175 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.27 关联字段无感打磨：可编辑已有记录 / 不误新建 / 去弹窗（2026-08-24）

**1. 修改关联不再误新建**：
- 根因：父侧关系字段的搜索候选只含「未挂靠」记录，输入已存在（已挂靠）记录名搜不到 → 只能点「+ 新建」产生重复记录；
- 修复：候选改为**全部记录**（含已挂靠），输入已有名字即可搜到并选中（覆盖关联，幂等），不再误新建。

**2. 可编辑已关联记录本身**：
- 详情字段卡已关联值旁新增 `›` 入口 → 下钻打开该记录详情（桌面侧栏/移动整页），在记录内原位编辑字段；
- 只读/不允许编辑的字段后续按字段定义标记（自定义/预设/警告），本轮保留现有只读列。

**3. 表格关系列去弹窗去铅笔**：
- 移除表格关系列的铅笔图标与 BottomSheet 弹窗；
- 点击关系列单元格 → 直接打开该行详情（桌面右侧栏/移动整页），在详情里关系字段原位编辑，保存后表格即时刷新。

验证：analyze 0 error；单测 175 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.28 右侧栏记录详情页（仿 Twenty）设计评审（2026-08-24，等待确认）

**背景**：用户要求按 Twenty 成熟模式重构 CRM 右侧栏：二级 Tab + 主页分区（标题行原位编辑 + 主字段区块 + 关联字段区块）+ 关联记录打开子详情页（带父上下文、父 ❌ 子 ←）。随后用户指示：**默认遵照 Twenty 开发思路与方法**（不考虑编程语言差异），有更优解才提出。

**本步产出（仅文档，无代码）**：
- 精读 Twenty 本地源码（`E:\Dev\twenty\packages\twenty-front\src`）：
  - `RecordShowContainer` / `CommandMenuRecordPage`：整页与右抽屉复用同一记录容器，仅 `isInRightDrawer` 切换布局；
  - `useRecordShowContainerTabs` + `BaseRecordLayout`：Tab 为声明式布局（position/hide 条件/cards），**右抽屉把 Fields+Timeline 合并为 Home，移动端不合并（5 Tab）**；
  - `ShowPageSubContainer`：SummaryCard（标题行原位编辑）+ TabList + 卡片渲染 + 右抽屉 Footer；
  - `FieldsCard` / `RecordDetailRelationSection`：主字段 PropertyBox 原位编辑；每个关系字段一个独立区块（标题+计数+列表+⊕/✎）；**关联记录 ⌄ = 就地展开全部字段编辑，chip = 打开完整详情**；
  - `useAddNewRecordAndOpenRightDrawer`：新建关联 = 先建记录并写父外键 → 再打开右抽屉详情继续编辑；
  - Twenty **没有嵌套详情栈**；子详情页 + ← 返回为本项目适配（无整页路由时的返回必需），嵌套深度默认 1、可配 3。
- 新文档：`docs/CRM右侧栏记录详情页-仿Twenty设计.md` v1.1（默认 Twenty 方案：源码实证 + 11 项决策映射表 D1–D11 + 落地设计 + P0/P1/P2 任务分解 + 5 个待确认点）。

**状态**：🟡 等待用户确认 5 个待确认点后进入 P0 开发（预计 3 迭代）。

### 9.29 记录详情页（仿 Twenty）P0–P2 落地（2026-08-24，feat/local-crm-v1）

用户确认全部推荐方案后，按 P0 → P1 → P2 三批落地：

**P0 详情结构 + 内层 Tab**（`318f71e`）
- 新增 `CrmRecordDetailShell`：顶部栏（根 ❌ / 子 ← + 附件/删除）+ Summary 标题行（标签字段原位编辑、失焦保存）+ 二级 TabList；
- 新增 `crm_record_detail_tabs.dart`：`CrmDetailCardType` 卡片模型 + Tab 配置（对应 Twenty RecordLayout）；右抽屉把 Fields+Timeline 合并为「主页」（主页/任务/笔记/文件），移动端不合并（字段/时间线/任务/笔记/文件）；
- `CrmEntityDetailView` 改为按卡片渲染（fields/timeline/tasks/notes/files），桌面侧栏与移动整页统一走 Shell；跟进表单移入时间线卡。

**P1 关联交互**（`708707c`）
- 关联记录 chevron **就地展开**：新增 `CrmRecordInlineFields`，展开显示该记录全部字段、原位编辑、失焦保存（Twenty 默认交互）；
- 点记录名称打开**子详情页**（同 Shell + ← 返回 + 父上下文「来自 客户 · XX」），嵌套上限 `kCrmDetailMaxDepth=3`，超出回落就地展开；
- **新建并关联**：创建成功写父外键后自动打开新建记录的子详情页继续编辑（`_PendingDetail` 帧序安全）；
- 关联区块记录「…」菜单：解除关联 / 删除（确认）；分组头「All (n)」→ 关抽屉 + 主 Tab 切换目标对象 + 按父记录名过滤；
- 抽取 `loadCrmEntityCache` 共享加载器（下钻/就地展开/移动端复用，去除移动页重复实现）。

**P2 打磨**（本轮）
- 移动端下钻：返回后父详情自动刷新 + 嵌套深度上限；
- Tab 按对象显隐覆盖扩展点（`kCrmDetailTabHides`，当前无内置规则）；
- 桌面**整页双栏**记录视图（左列 Summary+Fields 360px / 右侧 Timeline·任务·笔记·文件 Tab），右侧栏 header「打开整页」入口，整页内下钻为新的整页（深度同步 3 层）；
- 冒烟/行为测试新增：Shell 桌面/移动/整页/父上下文 4 例 + 就地展开编辑 2 例。

验证：analyze 0 error（仅存量 RegExp info）；单测 182 全绿；Windows 构建 ✅（mood_diary.exe）。

### 9.30 右侧栏页面细节打磨（2026-08-24，feat/local-crm-v1）

**1. 两行合一**：移除原第一行标题栏（类型图标/标签/父上下文独立行），Summary 标题行上移升级为唯一头行：`❌/← + 头像 + 名称（原位编辑） + 元信息行（父上下文 · 更新于…） + 打开整页/附件/删除`；桌面整页模式保留左列 Summary+Fields 布局不变。

**2. 关联分类独立区块**：
- 关联内容从主字段卡中拆出，每个关联类型一个独立区块（标题 = 类型名 + 计数 + All(n) + ⊕ 添加 + 就地展开/…菜单）；
- **主关联必显**：客户主页第二块 = 联系人，联系人主页第二块 = 客户（`kPrimaryRelation`，互斥且始终存在）；
- 其他关联（商机/合同/报价/回款/发票/质保等）：有内容才显示；底部「添加区块」下拉可把任一标准关联类型固定为新区块（空态 + ⊕），非主关联区块可 × 隐藏；
- 子侧单值关系（如机会的客户/联系人）同样独立成区块，原位搜索/新建/清除/打开。

**3. 核心信息区字段可配置**：
- 主字段卡标题改为「核心信息（可见/总数）」；
- 每对象预置核心字段集 `kDetailCoreFields`（5–7 条，按表内容确认），默认只显示核心字段；
- 右上「字段设置」弹窗勾选显隐（持久化 `crmDetailFieldsHidden_<type>`，已加入 PrefUtil 前缀白名单）；「展开全部/收起」临时切换；
- 标签区块并入核心信息卡底部。

验证：analyze 0 error；单测 184 全绿（新增核心字段子集/展开、添加区块 2 例）；Windows 构建 ✅。

### 9.31 关联交互与字段对齐打磨（2026-08-24，feat/local-crm-v1）

精读 Twenty 源码确认关联字段交互（`RelationToOneFieldDisplay` + `RecordChip`：非空关联值以可点击 chip 展示，点 chip 打开记录详情；编辑/更换走独立入口），对齐实现：

**1. 核心信息字段「冲齐」开关**：
- 字段设置弹窗新增开关「与表格列显示对齐」，**默认开启**：详情核心字段 = 当前表格显示列（表格未自定义列时 = 全部可编辑字段）；
- 关闭开关后进入「各自定义」：默认回退各表预置核心字段集（5–7 条），复选框可独立勾选显隐并持久化（`crmDetailFieldsHidden_<type>`）；
- 持久化同步开关 `crmDetailFieldsSync_<type>`（已加入 PrefUtil 前缀白名单）。

**2. 添加区块「动态持久化」**：
- 空关联区块固定仅本次会话（关闭详情即消失，不持久化）；
- 成功添加关联内容后，有内容区块**自动显示**（数据驱动），下次打开必然可见——空值不持久化、非空即持久，由数据动态确认（行为测试覆盖）。

**3. 关联字段空值判断 + 点击交互**：
- 所有关联字段按「是否为空值」分流：**非空关联值 = 可点击链接**（主色加粗），点击直接打开关联记录详情（下钻），不再误入搜索/选择列表；
- 更换/编辑走独立铅笔入口进入搜索选择；空值点击直接进入搜索（可新建）；
- 父侧关联列表记录项保持「点击下钻 + chevron 就地展开」，与 Twenty 一致。

验证：analyze 0 error；单测 185 全绿（新增同步开关流程、空区块不持久化/有内容自动显示、关联值点击下钻 3 例）；Windows 构建 ✅。

### 9.32 智能表格细节：复选框首列 / 排序移入右键菜单 / 差异自查（2026-08-24，feat/local-crm-v1）

**1. 复选框列固定首列**：
- 勾选列固定（`frozen: start`）为表格首列，宽度锁死 36px（`minWidth=36` + `suppressedAutoSize`，防拖拽/自动缩放）；
- 表头叠加**同尺寸复选框**：全选 / 部分选中（三态）/ 取消全选；
- 用背景色细条隐藏复选框列与首数据列之间的列间表格线（`IgnorePointer`，不挡交互）。

**2. 排序移入标题行右键菜单**：
- 所有数据列 `enableSorting: false`——标题后不再显示升降序箭头、点击标题不再排序；
- 自定义列菜单委托 `_CrmColumnMenuDelegate`：菜单顶部为「升序 / 降序」，并保留冻结/自动适配/隐藏列/设置列/筛选等默认项；
- **标题行右键**直接弹出该菜单（自动定位列；标题右侧调整区由 pluto 自身处理避免双重菜单）；标题右侧菜单图标点击同样可用；
- 排序后仍由 pluto 显示排序指示箭头（与 Twenty 一致）。

**补充（用户实测反馈）**：
- 取消所有列标题右侧「三条杠」菜单/调整图标（`enableContextMenu: false` + `enableDropToResize: false`，含复选框列/操作列）；
- 列操作（升序/降序/冻结/隐藏/筛选等）全部收敛到标题行右键菜单；右键不再跳过标题右侧区域；
- 新增 `crm_smart_table_test.dart` 3 例：无三条杠图标、右键菜单含升降序、表头全选/取消全选。

**3. 差异自查（对照 Twenty）**：
- 空态提示、批量选择操作条、列菜单（冻结/隐藏/筛选）、行双击下钻、单元格失焦/防抖保存等已与 Twenty 对齐，本轮未重复改动；
- 顺带修复：拖拽列排序持久化时过滤掉 `__select__/__actions__` 伪列，避免污染列设置。

验证：analyze 0 error；单测 185 全绿；Windows 构建 ✅。

### 9.33 选中批量操作条上移 Tab 导航行（2026-08-24，feat/local-crm-v1）

**调整**：
- 移除表格卡片页内的「已选 N 条 / 删除 / 导出 / 清除」行；
- 批量操作条上移到**最上方 Tab 导航条同一行、靠右对齐**（勾选后出现）：
  - `CrmTableController` 新增 `batchAction` 广播（delete/export/clear），首页按钮 → 当前激活表格页消费执行；
  - 各 Tab 勾选数量通过 `onSelectionCountChanged` 上报首页（`_selectionCounts` 按对象类型记录，切 Tab 自动切换计数）；
  - TabBar 放入 `Expanded` 并与右侧操作条同行：空间不足时 Tab 导航条保持横向滚动，无遮挡；
  - TabBar 自带底部分割线，与 Tab 卡片页分割清晰。

验证：analyze 0 error；单测 188 全绿；Windows 构建 ✅。

### 9.34 CRM 与 Twenty 差异全局扫描与表格/详情美化（2026-08-24，feat/local-crm-v1）

- 产出 `docs/CRM与Twenty差异扫描与改进清单.md`：逐项核对表格、右侧栏详情、一致性保障（全部对象/自定义对象共用组件，客户表特殊要求自动复用）；
- 本批改进：
  1. 数字/金额列右对齐 + 表头对齐（Twenty 数字列惯例）；
  2. 表格当前行轻微高亮（rowColorCallback）；
  3. 列宽跨会话记忆（`crmTableWidths_<type>`，防抖持久化，已入白名单）；
  4. 长文本单元格 hover Tooltip；
  5. 详情字段行 hover 高亮；
- 记录已知取舍：列宽拖拽因“无标题图标”要求暂禁（右键自动适配 + 宽度记忆补偿）、行 hover 以当前行高亮替代等。

验证：analyze 0 error；单测 188 全绿；Windows 构建 ✅。

### 9.35 移动端启动卡住修复（2026-08-24，feat/local-crm-v1）

**根因排查**：启动动画停留 = `main()` 中 `_initSystem()` 的 await 链未完成。链路中多个**平台通道调用无超时**，任一在 Android 上无响应都会永久卡在启动页：
- `AuthUtil.canCheckBiometrics()`（local_auth，首次启动 `setDefaultValues` 必经）；
- `PackageUtil.getPackageInfo()`（package_info_plus，每次启动取版本号）；
- `FileUtil.initCreateDir()`、`ThemeUtil().buildTheme()`、`_findLanguage()`。

**修复（启动加固）**：
- 生物识别检测 3s 超时兜底（失败置 false），不再阻塞首次启动；
- 版本信息 5s 超时兜底（失败用占位版本）；
- 默认值初始化 15s、目录创建 10s、Hive 10s、主题 10s、Pref 15s、数据库 20s 全部超时保护；数据库异常时**切换内存库兜底**保证可进界面（本次会话数据不持久，日志记录）；
- `_findLanguage` 5s 超时兜底（失败用英文 locale）；
- 首启/锁屏/字号/主题读取全部改为安全默认值（消除 `!` 空值崩溃）。

验证：analyze 0 error；单测 188 全绿；Windows 构建 ✅。Android 需重装后验证（建议 `adb logcat` 观察初始化日志）。

### 9.36 移动端三处打磨（2026-08-25，feat/local-crm-v1）

**1. CRM 首页状态栏对齐**：`CrmHomePage` 外层包 `SafeArea`，移动端顶部不再顶到系统状态栏（信号/电量区域与其它模块一致；桌面无副作用）。

**2. 表格第一列冻结开关**：
- 设置页「CRM 设置 → 通用」新增「表格第一列冻结」开关（默认开启，`crmTableFreezeFirst`）；
- `CrmSmartTable` 按开关决定复选框列是否冻结；关闭时隐藏表头全选方框与列间线遮罩，避免横向滚动时“方框不跟随列”的错位；
- 名称列冻结保留（Twenty 首列惯例）。

**3. 移动端数据不持久化根因修复**：
- **根因**：Android 构建 `abiFilters` 仅 `arm64-v8a`，x86_64 模拟器上缺少 `libsqlite3.so` → 磁盘库打不开 → 启动兜底切内存库 → 数据重启即清空；
- **修复**：`abiFilters` 增加 `x86_64`（真机 + 主流模拟器）；已构建 APK 验证同时包含 `arm64-v8a`/`x86_64` 的 sqlite3 与 rust 库；
- 配套诊断：数据库兜底时记录原因到 `logs/startup.log`（release 也可查），并在 CRM 首页顶部显示错误横幅「数据库初始化失败，当前数据不会保存」；
- 顺带记录：构建提示插件建议 NDK 28.2.13676358（当前 28.0 可正常构建，暂不升级以免缺本机 SDK）。

验证：analyze 0 error；单测 188 全绿；Windows 构建 ✅；Android debug APK 构建 ✅（含双 ABI 原生库）。

### 9.37 数据库迁移幂等修复（2026-08-25，feat/local-crm-v1）

**设备日志定位**：`SqliteException(1): duplicate column name: currency`，语句 `ALTER TABLE crm_contracts ADD COLUMN currency` —— v15→v16 迁移在列已存在时重复 `ADD COLUMN`，导致磁盘库打不开 → 启动兜底切内存库 → 数据不持久化。

**修复**：新增 `_addColumnIfMissing`（迁移 helper）：先 `PRAGMA table_info` 检查目标列，已存在则跳过 `ADD COLUMN`；v16 五个金额表的 currency 列全部走该 helper。`createTable` 经确认本身幂等（IF NOT EXISTS），无需改动。

**配套**：保留内存库兜底 + 首页红色横幅 + `logs/startup.log` 诊断（9.36），迁移正常后兜底不再触发。

验证：analyze 0 error；单测 188 全绿；Android debug APK 重建 ✅。

### 9.38 表格首列融合 + 每表冻结开关 + 列标题修正（2026-08-25，feat/local-crm-v1）

**1. 复选框与首列融合（pluto 原生 `enableRowChecked`）**：
- 移除独立 36px 复选框列与自绘表头全选遮罩，改用 pluto 原生能力：复选框内嵌首列单元格（与内容同排）、表头自动渲染全选复选框（与标题同排）——即用户参考的实例效果；
- 选中状态双向同步：外部 selectedIds → `setRowChecked`（编程不触发回调，无循环）；用户勾选 → `onRowChecked`（单行/全选事件）回写外层；
- 列间线遮罩随之移除（不再有独立复选框列）。

**2. 冻结规则统一**：
- 默认**只冻结首列**（复选框融合其中），移除对 `name/title` 的硬编码冻结（修复联系人「职位」列被误冻结）；
- 机会/合同/产品等表此前无冻结列的问题随“首列冻结”默认开启而统一；
- 「首列冻结」改为**每表独立开关**：列设置面板新增开关（`crmTableFreezeFirst_<类型>`），设置页保留全局默认值；关闭后可用列菜单右键自定义冻结。

**3. 列标题按字段定义**：
- 表格列标题改为取字段定义的 `label`（联系人 `title` 正确显示「职位」，不再显示内置“标题”）；
- 内置推断标题仅作缺省回退。

**4. 样式统一**：行高/字体/颜色/对齐/选中高亮集中管理于 `CrmSmartTable`（所有表共用），差异项（首列冻结）走开关。

验证：analyze 0 error；单测 188 全绿（表格组件测试适配融合复选框）；Windows 构建被运行中 exe 锁定（代码层已过），Android debug APK 构建 ✅。

### 9.39 报价/发票首列复选框“单独一列”修复（2026-08-25，feat/local-crm-v1）

**根因**：表格 `autoSizeMode: scale` 按容器等比缩放列宽；报价/发票首列（quoteNo/invoiceNo）默认仅 140px，缩放后复选框（固定约 30px）几乎占满列，内容被挤没 → 视觉上像“复选框单独一列”（name 列 220px 所以融合正常）。

**修复**：首列（复选框融合列）统一默认 200px、最小 180px，并 `suppressedAutoSize` 不参与 scale 缩放（列宽记忆优先，仍受最小宽度约束）；所有表的首列表现一致。

验证：analyze 0 error；单测 189 全绿（新增短首列融合测试）；Android debug APK 重建 ✅。

### 9.40 首列空白压缩 + 响应式首列宽度（2026-08-25，feat/local-crm-v1）

- **压缩复选框左侧空白**：首列 `cellPadding` 收紧为 `left:4 / right:8`（对照 Twenty 复选框列 30px 的紧凑风格）；
- **响应式首列宽度**：屏宽 ≥900 → 200px；600–900 → 160px（80%）；<600（移动端）→ 120px（60%）；用户记忆列宽优先，不参与 scale 缩放；
- 屏宽变化时自动重建列应用新宽度（build 期间直接刷新，不额外 setState）。

验证：analyze 0 error；单测 189 全绿；Android debug APK 重建 ✅。

### 9.41 首列表头/行对齐 + 复选框间隙 4px + 响应式宽度调整（2026-08-25，feat/local-crm-v1）

- **表头与行对齐**：首列改用自绘渲染——行内与表头均为「紧凑复选框（18px 图标）+ 4px 间隙 + 内容/标题」，左右起点、垂直位置完全一致；
- **响应式宽度调整**：≥900 → 300px；600–900 → 200px；<600（移动端）→ 180px（用户记忆列宽优先，不参与 scale 缩放）；
- 表头全选为三态图标（全选/部分/未选），点击切换全选/取消；不再依赖 pluto 原生 checkbox 布局（其 48px 点击区导致间隙过大）。

验证：analyze 0 error；单测 189 全绿（表格组件测试改为图标断言）；Android debug APK 重建 ✅。

### 9.42 首列宽度统一 + 设置项（2026-08-25，feat/local-crm-v1）

- **根因**：首列宽度此前优先读取各表历史保存的列宽（联系人/合同保存过旧窄值，且列宽拖拽已禁用），导致 PC 端各表首列宽窄不一；
- **修复**：首列宽度改为「全局设置值 → 屏宽自适应（300/200/180）」两级，**忽略各表历史保存值**，所有表首列统一；
- **设置入口**：CRM 设置 → 通用 → 「首列宽度」：输入像素值（0 = 自适应），全局生效。

验证：analyze 0 error；单测 189 全绿；Android debug APK 重建 ✅。

### 9.43 列宽锁定开关 + 首列宽度设置即时生效修复（2026-08-25，feat/local-crm-v1）

- **列宽锁定开关**：CRM 设置 → 通用 → 「锁定列宽」（默认开）：开启禁止拖拽调整列宽；关闭后非首列可拖拽调整（标题右侧低调拖拽图标），调好再锁定；首列宽度始终由「首列宽度」设置/自适应控制（保持各表统一）；
- **首列宽度设置“没效果”根因修复**：
  1. `CrmSmartTable.didUpdateWidget` 只对 items/fields 变化重建——现在 `firstColumnWidth / freezeFirstColumn / columnWidthLocked` 变化也重建；
  2. 设置页修改偏好后无通知——新增 `CrmPrefs.prefsVersion` 全局通知，CRM 首页监听并刷新，返回即生效（首列宽度/冻结/锁定均即时更新）；
- 列宽调整仍防抖持久化（解锁时）。

验证：analyze 0 error；单测 189 全绿；Android debug APK 重建 ✅。

### 🏁 收敛落地（2026-08-25，v2.8.0）

- `feat/local-crm-v1` 已本地快进合并进 `develop`（62a7f6d），工作区干净；
- 里程碑 tag `v2.8.0` 已打；
- **远程推送受阻**：github.com:443 当前网络不可达（本机无代理、重试仍超时）；已生成离线包 `E:\Dev\moodiaryCRM-m5-v2.8.0.bundle`（含全部分支与 tag），网络恢复后 `git push` 或从 bundle 恢复即可。

### 🏁 里程碑 M5 收口：CRM 本地化 + Twenty 对齐 + 移动端稳定（2026-08-25，v2.8.0+91）

- 版本固定：pubspec / Android versionName 2.7.3 → **2.8.0**（versionCode 90 → 91）；git tag `v2.8.0`；
- 成果文档：`docs/里程碑-M5-CRM本地化与Twenty对齐.md`（本地模型 / 详情页 / 智能表格 / 移动端稳定 / 决策 / 验证 / 遗留）；
- 最终快照：analyze 0 error；单测 188 全绿；Windows ✅；Android APK ✅（arm64+x86_64，设备实测数据持久化正常）；
- 下一阶段：进入新一轮细节完善（以用户后续清单为准）。

### 9.12 网格即时刷新 + 点击面板外保存退出（2026-08-23）

**根因**：pluto_grid 的 `didUpdateWidget` 只更新配置，**不应用列/行变化**——
列设置保存后旧网格仍在，需切页重建才生效（所有 Tab 通用）。

**修复**：
- CrmSmartTable 增加 `refreshToken` 驱动重建（key 变化强制新网格）：列设置保存、
  新增、删除、侧栏编辑、刷新、外部 reload 均触发；**网格内单元格编辑不重建**（避免丢焦点/滚动）；
- 统一走 `_refreshGrid()` 的外部变更点：列设置（面板+移动弹窗）、新增（面板+弹窗）、
  删除、详情面板编辑、toolbar 刷新、reloadToken；
- **点击面板外**：透明点击层拦截 → 等失焦自动提交完成（150ms）→ 关闭面板 → 重建网格刷新。

验证：analyze 0 error；单测 168；Windows 构建 ✅。

### 9.8 S5 收尾：附件 / 提醒 / 日历联动（2026-08-23，分支 feat/local-crm-v1）

**schema v14**：

| 表 | 说明 |
| :-- | :-- |
| CrmAttachments | 附件（多态关联；文件存应用文档目录 `Attachments/CRM/<类型>/<id>/`，库内只记路径，`CrmAttachmentStore` 复制/删除） |
| CrmReminders | 提醒（手动/自定义；类型 paymentDue·warrantyExpire·followUp·contractExpire·custom，完成标记） |

**功能**：
- 实体详情页附件区：添加（file_picker）→ 复制入沙盒 → 列表 → 打开（系统应用）→ 删除（记录+文件）；
- Tab 新增「提醒」：手动提醒 CRUD + 完成；
- **日历联动**：TodoAggregator 并入 CRM 日程——未完成提醒、跟进计划（scheduledAt）、回款计划（planDate）、到期提醒（dueReminders：回款/合同/质保）均投射到日历待办视图（只读，防误勾选）；
- OS 级本地通知暂缓（需评估 flutter_local_notifications 的 Windows 支持，列为 S6 可选）。

验证：analyze 0 error；单测 164（新增附件/提醒 1 例）；Windows 构建前次已通过（本次 App 运行中未覆盖构建）。

---

## 🎯 新一轮细节打磨（首页 / 详情页 / 编辑页）（2026-08-26，feat/local-crm-v1）

> 设计/差异分析+清单见 `docs/首页-详情页-编辑页打磨清单.md`（对照 Twenty + 闪念贝壳块视图样式）。
> 本轮策略：**先修好「三种首页视图」的进出入口一致性 + 块视图按图打磨**，再逐批做详情页 / 编辑页。

### 批次 1：块视图进出入口修复 + 按图打磨（已完成）

**问题**：首页「块视图」每张卡片用 `Card.outlined > ListTile`，**没有接 `onTap`**，点击无反应；而列表 / 网格视图都走 `BasicCardLogic.toDiary` 进入中间详情页（SmartCanvasPage）。三视图入口不一致。

**修改**：
- 新增 `lib/components/diary_card/block_diary_card_view.dart`（`BlockDiaryCardComponent`），按闪念贝壳单块样式渲染：顶部类型色条（text/todo/image/ai/entity/chart/code 各配色）+ 更新时间（`M月d日 HH:mm`）+ 标题（`block.meta.title` 优先，回退所属日记标题）+ 内容预览（`EllipsisText` 多行省略，非文本块前置类型标签）+ 底部 `#标签`（日记标签，无则显示 `#类型`）。
- `diary_tab_view_view.dart` 的 `buildBlock()` 改用该组件并接入 `onTap → toDiary(diary)`，与列表 / 网格一致进入 SmartCanvasPage；删除旧 `_blockIcon` 死代码；空态文案改为用户友好话术「还没有智能块，点右下角 + 记下第一篇吧」。

**效果**：块视图点击卡片 → 打开所属日记的中间详情页；卡片样式贴近图片的「色条 + 时间 + 标题 + 预览 + #标签」。

**验证**：`flutter analyze` 0 error（仅存量 `crm_entity_detail_view.dart:518`、`theme_util.dart:337` 两条 info，非本次改动）；`flutter test` 结果见下；Windows/Android 构建验证见下。

**最终验证（2026-08-26）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅（`build\windows\x64\runner\Debug\mood_diary.exe`）；
- `flutter build apk --debug`：✅（`build\app\outputs\flutter-apk\app-debug.apk`）。

> 注：本机沙箱内 `flutter`/`dart` 进程会被拦截，需在沙箱外（升级权限）运行 analyze/test/build；本次已全部通过。

### 批次 2：详情页分区重做（参照闪念贝壳）+ 编辑页返回自动保存（进行中）

**详情页 SmartCanvasPage 分区（按用户参考图）**：
- 按 `block.meta.isAi` 分上「笔记区」（非 AI 卡片）与下「AI 交互区」（AI 卡片），两区之间用「── 内容由 AI 生成 ──」短横线分隔；
- 笔记卡样式：顶部类型色条 + 来源徽标 + 相对时间（刚刚/x 分钟前/…）+ 内容 + 底部 `#标签` + 复制/⋮菜单（编辑/AI 处理/转待办/删除）；
- 「+ 追加笔记」按钮：笔记区左下角，作为笔记列表末尾项随新笔记下移；点击聚焦底部追加输入条（`AppendInputBar` 改为公开 State + `_focusNode` + `focusInput()`）；
- AI 卡样式：「已生成卡片：<模板>」+ 标题 + 内容 + 查看更多/复制/删除（流式中显示停止、未完成可继续生成）；
- 色条一一对应：抽公共 `blockTypeColor`/`blockTypeLabel`/`blockSourceLabel` 到 `lib/features/block/block_visuals.dart`，首页块视图卡片与详情页笔记卡共用；
- P0 虚拟化 + 桌宽：卡片列表改 `SliverList.separated`（按 `blockId` 建 key），桌面（>1024px）主列约束 720px 居中，移动端全宽；
- 移除开发项：「测试同步事件流」移入 `kDebugMode` 下显示；清理死代码：删除 `card_header.dart`/`card_footer.dart`。

**编辑页返回自动保存**：
- 脏标记 `_dirty`（内容/标题/心情/时间/分类/标签改动置位，保存成功后清零）；
- `handleBack` 双击返回：脏 → 失焦 + `saveDiary` 再返回；无改动 → 直接返回；无需弹窗确认；
- Toast 文案改为「再点一次返回（有改动会自动保存）」。

**最终验证（2026-08-26）**：
- `flutter analyze`：0 error（仅存量 2 条 info：`crm_entity_detail_view.dart:518`、`theme_util.dart:337`）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅；
- `flutter build apk --debug`：✅（`build\app\outputs\flutter-apk\app-debug.apk`）。

### 批次 3：首页导航统一 + 编辑页保存反馈/入口图标（已完成）

**首页导航一致性**：
- 桌面 `NavigationRail` 与底部 `NavigationBar` 图标统一为 Material 图标（日历 `calendar_month`、媒体 `photo_library`、设置 `settings`），消除 Material/Unicons 混用；
- CRM、AI 文案走 `l10n`（新增 `homeNavigatorCrm`/`homeNavigatorAi`，zh/en ARB + 生成 getter），与日记/日历/媒体/设置一致；
- 移除两处 `unicons` 引用。

**编辑页**：
- 保存反馈轻量化：全屏 `LottieModal(cat)` → AppBar 保存按钮在保存态变转圈（`isSaving` 改为 `RxBool`）并防重复保存；成功 Toast 保留；
- 详情入口图标语义：`keyboard_command_key` → `info_outline_rounded` + tooltip「日记信息」（打开日期/天气/分类/标签/心情弹层）。

**最终验证（2026-08-26）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅（`mood_diary.exe`）；
- `flutter build apk --debug`：✅（`app-debug.apk`）。

---

## 批次 11：细节收尾（2026-08-28）

> 新对话交接分析：分支 `feat/local-crm-v1`（v2.9.0+92，领先 origin 2 提交）；
> 对照「首页-详情页-编辑页打磨清单」，P0/P1 已基本落地（编辑页返回自动保存、
> AppBar 保存转圈、详情页虚拟化 + 720 阅读宽度、卡片尾部收敛、导航/FAB 两端统一等），
> 本轮补两处剩余 P2 收尾项。

| 项 | 说明 |
| :-- | :-- |
| 编辑页桌面阅读宽度 | `buildContent` 正文编辑/预览统一约束 **760px 居中**（`lib/pages/edit/edit_view.dart`），移动端保持全宽 |
| 详情页空态引导 | 空态从纯文本升级为「图标 + 文案 + **写第一张卡片** CTA」（`FilledButton.tonalIcon` → 打开追加笔记编辑器）（`smart_canvas_page.dart`） |

**验证（2026-08-28）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**。

### 下一轮候选（延续交接）
1. Rust `shutdown` 优雅收尾：`ffi_api.rs` 三条 `flutter_rust_bridge::spawn(while let...)` 无限循环持留 frb 运行时（需 frb 重新生成 + Rust 重建）。
2. 全局 ⌘K 命令入口（跨日记/CRM 搜索）。
3. 追加输入条语音/@ 占位弱化、AI 流式卡「转正」形态反馈等 P2 项。

---

## 批次 12：首页去标题 + 全局统一 Markdown 编辑器 + 笔记整合（2026-08-28）

### ① 首页记录列表只显示正文（三视图同步）
- 列表/网格/块三视图卡片**去掉标题行，只显示主要内容预览**：
  - `list_diary_card_view.dart`：删除 title EllipsisText，正文 maxLines 固定 4；
  - `grid_diary_card_view.dart`：删除 title 块；
  - `block_diary_card_view.dart`：删除 `_title` 文本行与 getter，保留正文预览。

### ② 全局统一为一个功能完整 Markdown 编辑器（EditPage）
- 新增 `lib/pages/edit/edit_arguments.dart`（`EditArguments`）：统一入口载荷
  （新建 / 整篇编辑 / 子笔记编辑 / 追加 / 笔记整合）；
- `EditLogic` 解析 `EditArguments`（兼容旧 `List`/`Diary` 参数），
  `saveDiary` 按模式分派：子笔记编辑 → 更新 Block + 刷新投影；
  追加 → `appendNote` 新建块；整篇/新建 → 原逻辑；
- 卡片点击编辑（`MarkdownEditAction`）→ 改走 EditPage；
- 详情页「追加笔记」→ 改走 EditPage（blockId='' 追加模式）；
- **删除冗余 `BlockMarkdownEditorPage`**（`editors/markdown_editor_page.dart` 已移除）；
- 入口统一：`home_logic` 新建、`diary_details` 编辑均改传 `EditArguments`。

### ③ 「编辑整篇日记」更名「笔记整合」（修复融合混乱）
- 详情页右上角菜单项改名「笔记整合」：打开 EditPage 时内容 = 日记聚合全文
  （主日记 + 子笔记已融合），保存后：
  - 融合全文写回主日记（`updateADiary`）；
  - **软删该日记下所有子笔记块**（新增 `IsarUtil.softDeleteNoteBlocksByDiary`，
    保留 AI 对话块）；
  - 返回详情页后 `init()` 由 initial 卡把主日记内容物化为一张卡——
    逻辑顺了：融合保存 → 删除子笔记 → 只剩一条主记录。

### 验证（2026-08-28）
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- MuMuPlayer 真机冒烟：`flutter run -d emulator-5556` 构建/安装/启动通过，
  首页列表只显正文（OCR 确认无标题行），进程无崩溃。

> 说明：子笔记编辑模式下标题改动会同步到日记标题；「日记信息」详情编辑
> 仍以整篇/新建模式生效（子笔记模式保存不落详情字段改动，后续可再细化）。

### 批次 12-1：修复「追加笔记」误填主笔记内容（2026-08-28）

- **Bug**：详情页点「追加笔记」打开的编辑器填充了当前主笔记（块）内容，
  保存后把主笔记内容也当新子笔记保存了。
- **根因**：追加模式（`blockId == ''`）在 `EditLogic._initFromArguments` 中
  走了 `_initEditDiary`，用 `diary.content`（聚合全文 = 主笔记内容）预填了正文。
- **修复**：追加模式在初始化后显式清空正文与媒体资源
  （`markdownTextEditingController.clear()` + 清空 image/video/audio 列表），
  保证打开的是**空白编辑器**，保存时只新增一个 `source=appended` 子笔记块。
- **验证**：`flutter analyze` 0 error；`flutter test` 189 全绿；
  MuMu 实机复测：追加笔记打开后字数 0、正文空白（修复前为 857 字 + 主笔记全文）。

### 批次 12-2：修复「展开全文」无响应 + 按主流交互完善折叠（2026-08-28）

- **Bug**：详情页长文卡片折叠正常，但点「展开全文」没反应。
- **根因（双层）**：
  1. `toggleExpand` 用 `update(['expand:...'])` 通知 GetBuilder，但列表外层是
     `Obx`，状态变更不触发重建；
  2. 改用 `RxSet` 后仍无效——GetX 的 `RxSet.contains()` **不注册 Obx 依赖**
     （源码直接 `value.contains`），add/remove 的 refresh 没有监听者。
- **修复**：`expandedIds` 恢复普通 `Set`，新增 `RxInt expandTick` 变更信号，
  `toggleExpand` 自增；详情页 `Obx` 读取 `expandTick.value` 注册依赖 →
  点击展开/收起后整列重建。
- **交互完善（主流 App 风格）**：
  - 折叠态：**完整渲染 Markdown**（不再 `substring(0,300)` 截断破坏语法），
    高度限制 220px + `ClipRect`/`OverflowBox` 裁剪 + **底部渐变遮罩**淡出；
  - 按钮居中（`展开全文 ▾ / 收起 ▴`），AI 区 footer 的重复「查看更多」入口移除
    （统一由 TextCard 内按钮控制）。
- **验证**：MuMu 实机闭环——折叠态 → 点「展开全文」卡片由 1083→1404 高、
  完整内容显示 → 滚到底点「收起」恢复折叠（按钮互切「展开全文/收起」）；
  `flutter analyze` 0 error；`flutter test` 189 全绿。

### 批次 12-3：折叠按钮精简为箭头图标 + 压缩底部空白（2026-08-28）

- **用户反馈**：展开/收起按钮距卡片底部空白过多；按钮只需保留上下箭头。
- **调整**（`text_card.dart`）：
  - 按钮由「文字 + 图标」（TextButton.icon）改为**仅箭头图标**
    （`Icons.expand_more_rounded ▾ / expand_less_rounded ▴`，18px，
    `InkWell + CircleBorder` 紧凑命中区，去掉 Material 默认 48×48 触摸区）；
  - 底部渐变遮罩 48px → **28px**；
  - 折叠态卡片高度实测 1107 → **1041**（IconButton 版），空白明显压缩，
    内容 → 箭头 → #标签 紧密排列。
- **验证**：MuMu 实机——折叠/展开/收起功能正常（点击箭头展开完整内容）；
  `flutter analyze` 0 error；`flutter test` 189 全绿。

### 批次 12-4：箭头与下方「标签+复制+更多」行间距压到 2px（2026-08-28）

- **调整**：
  - `smart_card.dart`：卡片 body（内容）与 footer 行间距 `SizedBox 6 → 2`；
  - `text_card.dart`：展开/收起箭头按钮底部内边距 `4 → 0`（保留左右/顶部触摸区）。
- **效果**：箭头视觉底到 footer 行 = 2px；折叠卡片高度 1413 → **1389**。
- **验证**：MuMu 实机截图确认紧凑；`flutter analyze` 0 error。

### 批次 12-6：修复子笔记/追加入口修改集合标题不生效（2026-08-28）

- **Bug**：从详情页 Block 卡（子笔记）或「+ 追加笔记」进入 EditPage，AppBar
  标题框修改后保存不生效。
- **根因**：`saveDiary` 分派到 `_saveEditBlock` / `_saveAppendBlock` 分支时，
  **未把标题输入框文本写回 `state.currentDiary.title`**，随后的
  `_syncTitleChange()` 因两个 title 恒等而直接 return，标题改动被丢弃。
- **修复**（按用户确认的语义：标题 = 集合标题）：
  - `edit_logic.dart`：子笔记/追加分支保存前统一
    `state.currentDiary.title = titleTextEditingController.text`，
    保存时写回所属 `Diary.title`，与正文（写回 Block）互不干扰；
  - `smart_canvas_logic.dart`：新增 `refreshDiary()`（重载 diary 并刷新
    AppBar 标题）；
  - `smart_canvas_page.dart`：`_openAppendEditor` 保存后调用 `refreshDiary()`
    保证标题即时刷新。
- **验证**：MuMu 实机——从 Block 卡「更多→编辑」进入，修改标题保存后
  详情页 AppBar 标题即时更新；`flutter analyze` 0 error；`flutter test` 189 全绿。

### 批次 12-7：首页长按/右键多选扩展到列表与网格视图（PC 端修复）（2026-08-28）

- **Bug**：PC 端首页长按/右键无法激活「已选 N 条 / 删除 / 取消」多选条。
- **根因**：多选删除**只在块视图**（`_BlockView`）实现，而 PC 端默认视图是
  **列表**（`homeViewMode` 默认 `list`），列表/网格卡片没有长按/右键多选能力。
- **修复**：把多选状态提升到 `DiaryTabViewComponent`（改 StatefulWidget），
  三个视图共享同一套多选逻辑：
  - `list_diary_card_view.dart` / `grid_diary_card_view.dart`：卡片新增
    `selectionMode / selected / onSelectionChanged / onLongPress` 参数，
    `onTap` 多选时切选择，`onLongPress` + `onSecondaryTap`（PC 右键）进入多选，
    选中态背景变 `primaryContainer`；
  - `diary_tab_view_view.dart`：操作条（已选 N 条 · 删除/取消）统一渲染在
    CustomScrollView 顶部，三视图共用；删除逻辑（回收站 + 软删子块 + refreshAll）
    由共享 State 管理；`_BlockView` 改为接收共享状态。
- **验证**：MuMu 列表视图长按 → 出现「已选 1 条 / 删除 / 取消」✓；
  `flutter analyze` 0 error；`flutter test` 189 全绿；
  `flutter build windows --debug` ✅（PC 端右键多选待用户实测确认）。

### 批次 12-8：多选操作条固定不随列表滚动（2026-08-28）

- **问题**：长按进入多选后，「已选 N 条 · 删除 / 取消」操作条是 CustomScrollView
  里的一个 sliver，列表滚动时会一起滚出屏幕，长列表时需要回滚才能点按钮。
- **修复**（`diary_tab_view_view.dart`）：`DiaryTabViewComponent` 改为
  `Column` 布局——操作条固定在列表**上方**（CustomScrollView 之外），
  列表在 `Expanded` 内滚动；激活多选后操作条始终可见，删除/取消随时可点。
- **验证**：`flutter analyze` 0 error；`flutter test` 189 全绿；
  `flutter build windows --debug` ✅。
  （注：MuMu 上 adb 长按模拟不稳定，多次未复现；移动端长按逻辑未改动，
  PC 端右键进入多选后操作条固定效果请以实机为准。）

### 批次 12-9：修正多选操作条——吸顶显示 + 激活才出现 + 紧凑行高（2026-08-28）

- **问题（用户实测）**：上一版用 `Column` 把操作条放在列表外，导致
  「全部/排序/筛选」行下方**常态出现一行宽空白**（Column 改变了
  TopRRectClipper / SliverOverlapInjector 的布局上下文）；且未激活多选也占位。
- **修复**（`diary_tab_view_view.dart`）：
  - 恢复原外层结构（`Padding > ClipRRect > CustomScrollView`），消除常显空白；
  - 操作条改为 **`SliverPersistentHeader(pinned: true)`** 吸顶：仅多选激活时
    添加到 slivers（`if (_selectionMode)`），不激活不占位；激活后吸顶固定，
    列表滚动时始终可见；
  - **紧凑行高 32px**（`minExtent/maxExtent = 32`），删除/取消按钮用
    `visualDensity.compact + shrinkWrap + minimumSize(0,26)`，只比文字略高，
    空白最小化。
- **验证**：`flutter analyze` 0 error；`flutter test` 189 全绿；
  `flutter build windows --debug` ✅（PC 端右键激活后吸顶效果待用户实测）。

### 🏁 收敛入库：批次 12 全量汇总（2026-08-29）

- 分支 `feat/local-crm-v1`，**领先 origin 13 个提交**，工作区干净；
  用户实测确认：**PC 端右键 / 移动端长按多选均正常**，显示
  「已选 N 条 · 删除 / 取消」吸顶条，未激活不占位、行高紧凑。
- 本批覆盖（自 v2.9.0 交接以来）：
  1. 编辑页桌面阅读宽度 + 详情页空态引导；
  2. 全局统一功能完整 Markdown 编辑器（删除冗余 `BlockMarkdownEditorPage`）、
     「编辑整篇」→「笔记整合」（保存融合全文后删子笔记）、首页三视图去标题；
  3. 追加笔记打开空白编辑器（不再预填主笔记内容）；
  4. 展开全文响应修复 + 主流折叠交互（渐变遮罩 / 箭头按钮 / 2px 间距 / footer 60% 高度）；
  5. 子笔记 / 追加入口修改标题写回集合标题；
  6. 首页长按/右键多选扩展到列表/网格视图 + 操作条 `SliverPersistentHeader` 吸顶固定。
- 验证基线：`flutter analyze` 0 error（存量 2 info）；`flutter test` 189 全绿；
  `flutter build windows --debug` ✅；`flutter build apk --debug` ✅（MuMu）。
- 下一轮候选（延续）：
  1. Rust `shutdown` 优雅收尾（`ffi_api.rs` 三条 `flutter_rust_bridge::spawn(while let...)`
     无限循环持留 frb 运行时）；
  2. 全局 ⌘K 命令入口（跨日记/CRM 搜索）；
  3. 移动端/桌面一致性复查（已知小项：详情页卡片内容区被 Markdown `SelectionArea`
     拦截点击，目前需走 footer「更多→编辑」进入编辑器）；
  4. P2.7.6 语音识别真实接入。

### 批次 13：卡片点击拦截修复 + Rust 优雅收尾 + 全局 ⌘K 命令入口（2026-08-29）

#### ① 详情页卡片点击被 SelectionArea 拦截（修复）
- **问题**：详情页卡片正文用 `MarkdownBlock(selectable: true)`（SelectionArea），
  与卡片 `InkWell.onTap` 竞争手势，点卡片内容进不了编辑器（需走 footer「更多→编辑」）。
- **修复**：`MarkdownContentView` / `TextCard` 增加 `selectable` 参数；
  SmartCard 的正文卡传 `selectable: false`（卡片点击优先），其它场景（AI 聊天、
  AI 主页、流式卡）保持可选中文本。

#### ② Rust `shutdown` 优雅收尾（治本）
- **根因**：`ffi_api.rs` 三条 `flutter_rust_bridge::spawn(while let Ok(event) =
  rx.recv().await)` 无限阻塞持留 frb 运行时，退出时只能靠 TerminateProcess 兜底。
- **修复**：
  - `event_bus.rs`：Sender 改为 `Mutex<Option<Sender>>` 容器，新增
    `shutdown_all()`——drop 所有 broadcast 发送端，`rx.recv()` 返回 `Closed`；
  - `ffi_api.rs`：新增 `shutdown()` FFI（调 `shutdown_all()`），三条循环在
    sink 失效或信道关闭时自动退出；
  - `flutter_rust_bridge_codegen 2.11.1` 重新生成绑定（新增 `shutdown()`）；
  - `main.dart` 退出清理：先 `rust_ffi.shutdown()` 通知循环退出，再
    `RustLib.dispose()`；
  - 测试：`drop_all_senders_closes_receiver` + `shutdown_all_clears_sender_boxes`，
    cargo test 18/18 全绿。

#### ③ 全局 ⌘K 命令入口（跨日记 / 子笔记 / CRM）
- 新增 `lib/features/command_palette/command_palette.dart`：
  桌面风格命令面板（顶部居中、640 宽、Esc/遮罩关闭），150ms 防抖实时搜索；
- `GlobalSearchResult` 增加 `diaryId`（block 结果可跳所属日记）；
- 结果点击：日记/子笔记 → 打开 SmartCanvasPage 详情；CRM → 回首页切 CRM 页签；
- `main.dart` 根组件注册 `Ctrl+K / ⌘K`（HardwareKeyboard）全局快捷键。

#### 验证（2026-08-29）
- `flutter analyze` 0 error（存量 2 info）；`flutter test` 189 全绿；
- `cargo test` 18/18 全绿；frb codegen 重新生成成功；
- `flutter build windows --debug` ✅；`flutter build apk --debug` ✅。

> 说明：卡片点击修复与 ⌘K 面板的最终交互请以 PC/真机实测为准；
> Rust shutdown 后退出应更快（不再依赖 TerminateProcess 兜底）。

### 批次 12-5：footer 行（标签+复制+更多）高度压缩到约 60%（2026-08-28）

- **原高度**：footer 行约 48px（PopupMenuButton/IconButton 默认触摸区主导）。
- **调整**（`smart_card.dart`）：
  - `_FootIcon`：IconButton 改用 `tapTargetSize: shrinkWrap` +
    `minimumSize/maximumSize 28×26` + `padding zero` + 图标 18→16；
  - 「更多」PopupMenuButton：同样紧凑化（iconSize 16 + shrinkWrap style）；
  - 行高由约 48px → **约 28px（≈60%）**，图标实测语义高度 120/144 → 78。
- **验证**：MuMu 实机截图确认紧凑；`flutter analyze` 0 error。

### 批次 5：追加记录修复 + 详情页 AI 交流输入框（已完成）

**① 修复「详情页追加产生新记录导致重复」**：
- 此前底部输入框直接追加内容（生成 block 后刷新投影）；现改为「追加笔记」按钮 → 打开 Markdown 编辑器（`BlockMarkdownEditorPage` 新增**新建块模式**：`blockId=''` 时走 `CanvasDatasource.appendNote`，在**既有日记**下新增 `source=appended` 的 text 块）——只落 Block、不新建 Diary，首页不会多出一条记录；
- 移除底部用于追加的 `AppendInputBar`（已删除组件），杜绝「追加」路径误入新日记。

**② 详情页底部输入框 → 纯 AI 交流**：
- 抽出共享组件 `lib/features/ai/widgets/ai_chat_input_bar.dart`（胶囊输入 + 可选 leading/trailing + 发送/停止），**常驻显示**、随时可输入、样式与首页快速输入框/知识库页一致；
- 详情页底部改用该输入框（左侧「记录问答」chip），`SmartCanvasLogic.sendChat` 把问题作为 `source=ai` 的 AI 卡，流式答复后保留为「对话卡」（不追加为笔记）；`isChatStreaming` 驱动停止按钮；
- **AI 知识库页（AiHomePage）同步复用** `AiChatInputBar`（替换其私有 `_buildInputBar`），两处输入框统一。

**③ 追加笔记按钮直连 markdown 编辑页**：点击「+ 追加笔记」打开 Markdown 编辑器，输入新内容保存即在原记录下生成新块。

**最终验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅（首次因调试版 exe 运行占用 WebView2Loader.dll 失败，进程退出后重建通过）；
- `flutter build apk --debug`：✅。

### 批次 6：AI 瀑布流对话 + 快捷输入框提交即清空退出 + 常驻输入框两态（已完成）

**① 修复「AI 交互产生新笔记记录」并改瀑布流**：
- AI 对话改为**内存瀑布流**（`SmartCanvasLogic.chat`：用户/助手消息，不落库、不刷新 diary 投影），杜绝「AI 答案变成一条新笔记/新记录」；
- 助手气泡下提供 **复制 / 更多（生成新笔记、重新生成）**：`生成新笔记`=把该回答存为 source=appended 的块；`重新生成`=重新请求该问题；流式中提供停止。
- 详情页 AI 区渲染瀑布流气泡（`widgets/chat_bubble.dart`）+ 已保存的模板 AI 卡。

**② 快捷输入框提交即清空并退出**：
- `QuickCaptureSheet` 改用共享 `SmartInputBar`；提交成功 → `_controller.clear()` + 清空 text/attachments + `Navigator.pop(true)`，下次 FAB 重新干净唤起。

**③ 常驻输入框两态（折叠/激活）**：
- 新增共享组件 `widgets/smart_input_bar.dart`：**折叠态一行**（左侧大模型选择 + @，中间「按住输入语音」可点按激活/长按语音，右侧语音/键盘互斥 + +，发送隐藏）；**激活态两层**（上层输入框按内容自动变高，下层功能栏 模型/@/语音/+/发送）。
- 详情页常驻、AI 知识库页用 `SmartInputBar(startActive:false)`（默认折叠）；快速收集用 `startActive:true`（进入即两行激活态）。删除旧 `AiChatInputBar`。

**最终验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅（`mood_diary.exe`）；
- `flutter build apk --debug`：✅。

### 批次 7：首页块视图分组多选删除 + AI 对话持久化瀑布流（已完成）

**① 修复「追加子笔记后首页重复」**：根因是首页块视图「一块一卡」，追加子笔记后多出一张卡（外观像重复记录）。现改为**一条笔记一张卡**：`BlockDiaryCardComponent` 按 `diary` 分组渲染，顶部为一排**子笔记色条**（数量=该笔记非 AI 的子笔记块数，无追加时显示 1 条），颜色与详情页块背景对应（复用 `blockTypeColor`）。

**④ 色条数量 = 子笔记数，增删即时刷新**：块视图 `_BlockView` 每次从 `getAllDiariesSorted` + `getAllVisibleBlocks` 读取并分组；新增/删除子笔记（即块）后重新加载，色条随之增减；删除整条记录也同步刷新。

**⑤ 首页记录块长按（PC 右键）多选 + 删除整条记录**：`_BlockView` 支持长按/右键进入多选，顶部显示「已选 N 条 · 删除 / 取消」；删除走 `IsarUtil.moveDiaryToRecycle`（软删 show=false，进回收站）+ `softDeleteBlocksByDiary` 同步软删子块，并 `DiaryLogic.refreshAll()`。

**② AI 对话持久化瀑布流**：修正上一批「AI 对话仅内存、退出即失」的问题——`sendChat` 现把**用户提问 + AI 回答都持久化为 `source=ai` 块**（`BlockMeta.role=user/assistant`），AI 区按 `role` 以上往下瀑布流渲染（`chat_bubble.dart`），无对话时回到该笔记详情即可恢复；AI 对话块**不并入笔记投影**（`MarkdownProjection.aggregate` 排除 `role` 非空块），与原文笔记分区。

**③ 去掉「AI 生成内容添加为笔记」**：助手气泡「更多」菜单移除「生成新笔记」，仅保留「重新生成」；后续更多功能待扩展。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅（`app-debug.apk`）；
- `flutter build windows --debug`：⚠️ **因调试版 `mood_diary.exe` 正在运行、锁定 WebView2Loader.dll 无法覆盖**（属环境/进程锁，非代码问题；analyze+test+APK 已全部通过）。关闭运行中的调试应用或释放锁后即可重建通过。

### 批次 8：首页块视图返回即时刷新 + Windows 窗口关闭彻底退出（已完成）

**① 修复「追加子笔记保存后，首页色条不即时更新」**：
- 根因：`_BlockView` 只在 `initState`/删除后重载，从详情页返回（追加子笔记后）不触发。
- 修复：`_BlockView` 监听所属 `DiaryTabViewLogic.state.diaryList`（RxList）变化即重载；从详情页返回时 `BasicCardLogic.toDiary` 的列表更新会触发监听 → 色条数量/颜色即时刷新，无需重启。

**② 修复「Win11 点 ❌ 关闭窗口后进程残留」**：
- 根因：`win32_window.cpp` 的 `MessageHandler` 未显式处理 `WM_CLOSE`，若 Flutter/自定义框架（bitsdojo）拦截了关闭消息，默认 `DefWindowProc` 销毁路径未走，`WM_DESTROY → PostQuitMessage` 不触发，主消息循环持存 → 进程不退出。
- 修复：在 `Win32Window::MessageHandler` 增加 `WM_CLOSE` 分支：`DestroyWindow(hwnd)`，确保走标准销毁 → `WM_DESTROY` → `PostQuitMessage` → 主循环退出、进程结束。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅；
- `flutter build windows --debug`：✅（`mood_diary.exe`，含 `WM_CLOSE` 修复，MSVC 编译通过）。

### 批次 8-3：窗口关闭强制退出 + 三视图同源收敛 + 首页筛选排序（已完成）

**① Win11 关窗前版仍残留 → 改为直接退出主消息循环**：
- 上一版在 `WM_CLOSE` 调 `DestroyWindow`（依赖引擎销毁，可能被后台线程/插件阻塞导致卡住）。现改为 `WM_CLOSE` 直接 `PostQuitMessage(0)`，不依赖引擎销毁——主消息循环必然返回 0 → `wWinMain` 返回 → **进程彻底结束**（窗口随之消亡）。

**② 三视图数据不一致 → 收敛为同一数据源**：
- 根因：块视图此前用 `getAllDiariesSorted + getAllVisibleBlocks`（全量、且过滤「必须有块」），与列表/网格的 `diaryList`（分页）不一致。
- 修复：块视图改为**直接使用 `DiaryTabViewLogic.state.diaryList`**（与列表/网格同源、同排序、同分页、同过滤），只额外取一次块数 `getAllVisibleBlocks` 用于色条计数。三视图记录数量、顺序、筛选完全一致；滚动到底仍共用分页触发器。

**③ 首页 tab 行加筛选/排序**：
- 新增 `DiarySort`（创建时间 ↓/↑、修改时间 ↓/↑、名称），首页 tab 行左侧加「排序」按钮（`Icons.sort` + 弹出多选菜单）；
- `IsarUtil.getDiaryByCategory(... sort:)` 支持按 `time/lastModified/title` 排序；`DiaryTabViewLogic` 三处查询、`DiaryLogic.changeSort`（持久化 `homeSortOrder` 并 `refreshAll`）接入；列表/网格/块三视图共用同一排序。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅；
- `flutter build windows --debug`：✅（含 `WM_CLOSE → PostQuitMessage` 修复）。

> 注：①的进程退出修改在 Win11 实机需再验证（无法在本环境运行 GUI）。若仍有个别场景残留，请补充现象（如退出方式：❌ / Alt+F4 / 任务栏关闭），我再精调。

### 批次 8-4：修复「首页排序运行时报 Key not in allowlist」（已完成）

- **出错现象**：`flutter run` 启动即报 `Invalid argument(s): Key "homeSortOrder" is not in PrefUtil allowlist/prefixes`，并级联出现两个巨大的 `RenderFlex overflowed by 98776/99898 pixels`。
- **根因**：上一批加了 `homeSortOrder` 排序键并 `PrefUtil.getValue/setValue`，但未加入 `PrefUtil.prefAllowList` 白名单 → `DiaryState.sort` 初始化抛异常 → 首页构建失败，异常渲染又引发溢出。
- **修复**：将 `homeSortOrder` 加入 `prefAllowList`，并在 `PrefUtil._initialize` 里为它登记默认值 `0`（= 创建时间 ↓）。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅；
- `flutter build windows --debug`：✅（`flutter run` 应不再报 Key 异常，排序按钮正常）。

### 批次 9：关闭进程即时退出 + 首页排序/筛选（已完成）

**① 「退出前先未响应、很慢很卡」**：
- 上一版 `WM_CLOSE → PostQuitMessage` 依赖引擎/插件线程收尾（可能阻塞），导致先"未响应"再缓慢退出。
- 改为 `WM_CLOSE → ::ExitProcess(0)`：**进程即时终止、窗口立即消失**，不再出现未响应等待（数据已实时落库，直接退出安全）。同时去掉 `ExitProcess` 后不可达的 `return 0;`（消除 warning-as-error C4702）。

**② 首页 tab 行排序右移 + 新增筛选**：
- 排序按钮从行左侧移到**右侧**（`Icons.sort`，多选菜单：创建时间↓/↑、修改时间↓/↑、名称）；
- 新增**筛选按钮**（`Icons.filter_alt`）：弹出「按标签多选」面板（`_DiaryFilterSheet`），选中标签后列表/网格/块三视图按标签（任一命中）过滤；有生效筛选时按钮高亮；支持「清除 / 应用」；
- 筛选状态存于 `DiaryLogic.state`（`filterTags`/`filterActive`），`DiaryTabViewComponent` 及 `_BlockView` 均按它过滤 `diaryList`，三视图一致。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅；
- `flutter build windows --debug`：✅（`ExitProcess` 即时退出，MSVC 编译通过）。

### 批次 10：按排查计划做「资源清理 + 优雅退出」（治根：退出残留 + 越用越卡）

> 依据：用户提供的 Flutter Windows 退出排查计划（root cause = 后台资源未释放阻塞引擎收尾）。

**退出清理架构**：
- 新增 `lib/utils/resource_cleanup.dart`（`ResourceCleanupManager`）：按注册**逆序**执行清理，单任务带超时，单项失败不阻断；
- `main.dart`：`_initSystem` 注册清理任务（`rust`→`RustLib.dispose()`、`db`→`IsarUtil.closeDatabase()`、`sync`→`SyncEventService.stop()`）；
- `Moodiary` 改为 StatefulWidget + `WidgetsBindingObserver`，重写 `didRequestAppExit`：先 `cleanupAll(3s)` 释放资源，再返回 `AppExitResponse.exit`；并设 **3 秒兜底 `exit(0)`**（清理/收尾仍卡则强制结束）。
- `IsarUtil.closeDatabase()`：关闭 Drift `NativeDatabase`（释放 native 线程）。

**治「越用越卡」的泄漏**：
- `SmartCanvasLogic` 的 `SyncEventService.syncEvents.listen` 订阅**未取消**（每次进详情页泄漏一条）→ 改为保存 `_syncSub` 并在 `onClose` 取消；
- `SmartCanvasLogic` 是手动 `Get.put`，路由关闭不会自动删 → 在 `SmartCanvasPage.dispose` 显式 `Get.delete<SmartCanvasLogic>(tag, force: true)`，连同上一条订阅一起释放。

**Runner**：恢复**标准净化路径**（`WM_CLOSE → DestroyWindow → WM_DESTROY → PostQuitMessage`，`main.cpp` 仍 `SetQuitOnClose(true)`），移除上一版的 `ExitProcess`；另加**8 秒兜底线程**（若引擎收尾被阻塞仍未退出则 `ExitProcess(0)`，杜绝无限残留）。

**验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build apk --debug`：✅；
- `flutter build windows --debug`：✅（含 RRunner 兜底线程 + 标准净化，MSVC 编译通过）。

> 注：①请在 Win11 实机验证「点 ❌ 应干净、快速退出、无未响应」，并观察 Console 中的 `🧹/✅/❌ 清理` 日志以确认资源释放顺序；②若仍有个别场景残留，请在退出前用 Process Explorer 看残留线程名（sqlite3_worker / tokio / dart::ThreadPool），我再据此精确定位。

### 批次 10-2：定位残留线程 → 兜底移到 `FlutterWindow::MessageHandler`（已建）

- **用户实测**：残留线程只有主线程 `mood_diary.exe!ILT+5970(wWinMainCRTStartup)` = 主消息循环没退，引擎收尾被阻塞。
- **根因**：上一版 8 秒兜底放 `Win32Window::MessageHandler`，但 Flutter 的 `HandleTopLevelWindowProc` 会**先消费 `WM_CLOSE`** 并提前 return，导致兜底**根本没执行**——这才是「反复仍残留」的真正原因。
- **修复**：把 8 秒兜底移到 `FlutterWindow::MessageHandler`（真正入口、`HandleTopLevelWindowProc` 之前），保证 `WM_CLOSE` 时必然启动兜底线程；8 秒后仍未退出则 `ExitProcess(0)`，杜绝无限残留。
- 结合 Dart 侧 `didRequestAppExit` 清理（关库/停 Rust/取消同步）+ 3 秒 `exit(0)` 兜底：正常情况下资源释放后引擎收尾完成、快速干净退出；异常时也有 3s/8s 双层强制退出。

**验证（2026-08-27）**：`flutter analyze` 0 error；`flutter test` 189 全绿；`flutter build apk --debug` ✅；`flutter build windows --debug` ✅（删被占用的 `mood_diary.pdb` 后通过）。

> 补充：若实机仍有「约 8 秒未响应才退出」的个别场景，深层原因几乎确定为 Rust 侧 `ffi_api.rs` 里三条 `flutter_rust_bridge::spawn(async move { while let Ok(event) = rx.recv().await {...} })` 的**无限循环**（无退出条件）持留 frb 运行时。需要新增 Rust `shutdown` 关闭事件渠道（frb 代码生成 + Rust 重建）才能真正优雅收尾——可作为下一步。

### 批次 10-3：定位 atexit 插件析构（D3D11 驱动）卡住 → 主循环后直接 `ExitProcess`（待重建）

- **用户提供深堆栈**：进程已进入 `exit()`（CRT 收尾），在 `flutter_inappwebview_windows_plugin` 的**静态 `InAppWebViewManager::graphics_context_` 析构（atexit）**释放 D3D11 `IDirect3DDevice` 时卡在 `igd10um64xe.dll`（Intel/WebView2 D3D11-on-12 释放挂起）。即：**主消息循环已退出，卡的是 atexit 里的插件 D3D11 设备释放**。
- **修复**：`windows/runner/main.cpp` 将主循环结束后的 `return EXIT_SUCCESS;` 改为 `::ExitProcess(0);`——**不再返回 CRT 的 `exit()`、跳过 atexit 表**，从而避开 `flutter_inappwebview_windows` 静态 `GraphicsContext` 析构在 D3D11 设备释放上的挂起 → 快速、干净退出。

**状态**：
- `flutter analyze` 0 error、`flutter test` 189 全绿、`flutter build apk --debug` ✅；
- `flutter build windows --debug`：✅（用户结束后 `mood_diary.exe` 后重跑，`main.cpp` 主循环后 `TerminateProcess(0)` 经 MSVC 编译通过）。

### 批次 10-4：`TerminateProcess`（真正根治）+ 禁用 WebView2 GPU（尽力而为）

**为何 `ExitProcess` 没用**：`ExitProcess` 仍会走 `RtlExitUserProcess → LdrShutdownProcess`，触发各 DLL 的 `DLL_PROCESS_DETACH`——`flutter_inappwebview_windows` 的静态 `GraphicsContext` 析构正是在此执行（释放 `IDirect3DDevice`），在部分 Intel/WebView2 环境卡死 `igd10um64xe.dll`。所以 `ExitProcess` 绕不开。

**修复（`windows/runner/main.cpp`）**：
- 主循环结束后改 `::TerminateProcess(GetCurrentProcess(), 0)`——**跳过 DLL 卸载（不跑 `DLL_PROCESS_DETACH`）**，插件静态 `GraphicsContext` 析构不会执行，D3D11 释放不会发生 → 进程立即、干净退出；
- 同时在 `wWinMain` 最开头 `SetEnvironmentVariableW("WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS","--disable-gpu")`（早于 Flutter/插件初始化，WebView2 惰性创建环境时读取），**尽力而为**关闭 WebView2 GPU，降低插件合成器 D3D11 设备的创建/释放风险。

> 坦诚说明：堆栈里的 D3D11 设备是**插件自身**用于合成 WebView 的 `GraphicsContext`（非 WebView2 渲染 GPU），`--disable-gpu` 主要关 WebView2 渲染，**不一定阻止插件建这个合成设备**；真正兜底是 `TerminateProcess`。两者叠加，退出可靠、快速。

**验证（2026-08-27）**：`flutter analyze` 0 error；`flutter test` 189 全绿；`flutter build apk --debug` ✅；`flutter build windows --debug` ✅（`--disable-gpu` + `TerminateProcess`，MSVC 编译通过）。

### 🏁 收尾：版本 v2.9.0+92（2026-08-27）

- 版本：pubspec `2.8.0+91` → **`2.9.0+92`**（Android `flutter.versionCode/Name` 随 pubspec 走）；
- 分支 `feat/local-crm-v1`；里程碑 tag `v2.9.0`；
- 本轮涵盖：首页三视图同源 + 排序 + 标签筛选 + 块视图多选删除、详情页分区（笔记区/AI 区）+ 块配色 + 追加直连 markdown、编辑页自动保存/轻反馈、快速收集/常驻输入框两态 + AI 瀑布流（持久化）、**桌面进程退出根治**（`TerminateProcess` 跳过 DLL 卸载 + `--disable-gpu` + `didRequestAppExit` 资源清理 + 兜底）等；
- 最终门禁：`flutter analyze` 0 error（仅存量 2 条 info）；`flutter test` 189 全绿；`flutter build windows --debug` ✅；`flutter build apk --debug` ✅；
- 用户实机确认：**点 ❌ 快速、干净退出，任务管理器 `moodiary` 进程消失**。

---

## 🧭 下一轮交接（新对话阅读）

- **分支/tag**：`feat/local-crm-v1`；tag `v2.9.0`；pubspec `2.9.0+92`。
- **已就绪**：首页三视图一致、排序/标签筛选、块视图多选；详情页笔记区/AI 区、块主题色、追加直连 markdown；编辑页自动保存/轻反馈；快速收集与常驻输入框两态；AI 瀑布流（持久化）；桌面退出根治。
- **本轮候选（待办/可选）**：
  1. **Rust `shutdown` 优雅收尾**：`rust/src/api/ffi_api.rs` 三条 `flutter_rust_bridge::spawn(while let ...)` 无限循环持留 frb 运行时——当前靠 `TerminateProcess` 跳过；若要真·优雅可加 Rust shutdown（frb 代码生成 + 重建）。
  2. **全局 ⌘K / 命令入口**（跨日记/CRM 搜索）。
  3. **编辑页头部 chips 交互、桌面端细节**、冲突卡轻提示等收尾项。
  4. 移动端与桌面端个别交互一致性复查。
- **验证基线**：`flutter analyze` 0 error；`flutter test` 189；`flutter build windows --debug` ✅；`flutter build apk --debug` ✅。
- **注意**：本机沙箱内 `flutter`/`dart` 需升级权限（沙箱外）运行；Windows 构建需先关闭运行中的 `mood_diary.exe`（否则锁 PDB/exe）。

### 批次 4：块配色体系 + 快速收集输入框按图重构（已完成）

**块配色**（首页块视图 ↔ 详情页块一一对应，均走 `lib/features/block/block_visuals.dart`）：
- 详情页块卡**去掉顶部颜色窄条**，改为「现代化分类背景色」：`blockAccent`（蓝/绿/紫/青/紫罗兰/琥珀/石板灰）+ `blockBackground`（accent 低透明度与 `surfaceContainerLow` 混合），随当前主题（亮/暗）自适应；
- 首页块视图色条沿用同一 `blockAccent`，与详情页对应块背景色一致；块数量天然对齐（同一批 Block）；
- 详情页块卡头部保留相对时间 + 来源徽标 + 待同步/冲突小图标（轻提示）。

**快速收集输入框**（FAB 打开的 `QuickCaptureSheet`，按参考图重构）：
- 改为「单一胶囊输入框」：`[模板chip 快速▾] [@] [输入/按住说话] [语音/键盘切换] [+附件] [发送(有内容时)]`，模板选择、@、语音模式、添加附件、发送全内嵌；
- 模板 chip 选中时高亮并显示模板名（仿「DS 快速」模型钮）；
- 「+」打开的附件改为**网格**（图片/音频/文档/其他文档，图标+文字，4 列）；
- 发送按钮：有文字或附件时显示，保存中转圈。

**剩余选项**：
- 编辑页顶部「类型/计时/字数」chips 从内容区叠层移到 AppBar 下方一行，避免遮挡首行；
- 桌面导航栏底部已显示应用名（沿用既有 `DesktopHomeFabComponent`）；
- 详情页块卡补「待同步/冲突」轻提示图标。

**最终验证（2026-08-27）**：
- `flutter analyze`：0 error（仅存量 2 条 info）；
- `flutter test`：**189 全绿**；
- `flutter build windows --debug`：✅（`mood_diary.exe`）；
- `flutter build apk --debug`：✅（`app-debug.apk`）。
